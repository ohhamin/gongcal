{
  "name": "ourcal-monorepo",
  "version": "0.4.16",
  "private": true,
  "scripts": {
    "web:dev": "npm --prefix web run dev",
    "web:build": "npm --prefix web run build",
    "web:lint": "npm --prefix web run lint",
    "app:doctor": "cd app && flutter doctor",
    "app:build:apk": "cd app && flutter build apk --release"
  }
}
