# OPENCLAW

## v.0.4.55 - 2026-06-08

### 업데이트 내용

- `/calendar` 페이지에서 `useSearchParams()`를 직접 사용해 Next.js prerender가 실패하던 문제를 수정했습니다.
- 위젯 월 이동 쿼리(`widgetMonth`)는 클라이언트 마운트 이후 `window.location.search`로 읽고 FullCalendar API로 이동하도록 변경했습니다.
- README/package 기준 버전을 `v.0.4.54`에서 `v.0.4.55`로 업데이트했습니다.

### 작업 메모

- Supabase 더미 환경 변수로 `npm --prefix web run build`를 실행해 `/calendar`를 포함한 static prerender 통과를 확인했습니다.

## v.0.4.54 - 2026-06-08

### 업데이트 내용

- 소셜 로그인 화면을 `100vh` 고정 레이아웃으로 중앙 정렬해 화면 자체 스크롤이 생기지 않도록 조정했습니다.
- 월간 캘린더에서 하루 이벤트를 최대 4개까지 노출하고, 5개 이상일 때만 `+N` 더보기가 나오도록 변경했습니다.
- 월간 캘린더와 위젯에서 타인 일정은 일정 제목 대신 소유자 이름만 표시하도록 조정했습니다.
- 위젯 스냅샷도 타인 일정 표시명/내 일정 여부/비밀 잠금 여부를 반영하도록 보강했습니다.
- 위젯 공휴일 3글자도 `...`로 줄어들지 않도록 이벤트 pill 좌우 여백을 줄였습니다.
- 위젯 헤더를 `< yyyy년 M월 >` 월 이동 컨트롤과 `나/그룹/그룹설정` 칩 형태로 재구성했습니다.
- 위젯 `<`/`>` 클릭 시 위젯 표시 월을 이동하도록 native 상태를 추가했습니다.
- 위젯 달력/월 제목 클릭 시 앱의 해당 월 캘린더 URL(`/calendar?widgetMonth=YYYY-MM`)로 열리도록 Flutter/WebView 초기 진입 처리를 추가했습니다.
- README/package 기준 버전을 `v.0.4.53`에서 `v.0.4.54`로 업데이트했습니다.

### 작업 메모

- Android AppWidget은 실제 드롭다운 UI가 제한적이라 `그룹설정`은 앱 그룹 화면으로 이동하는 칩으로 구현했습니다.

## v.0.4.53 - 2026-06-08

### 업데이트 내용

- 로그인 화면이 작은 화면 높이에서도 스크롤 없이 들어오도록 로고/간격/카드/버튼 높이를 `dvh`와 `clamp()` 기준으로 압축했습니다.
- Android 월간 캘린더 위젯 헤더에 상하 10dp 패딩을 추가하고, 캘린더 좌우 공백을 제거했습니다.
- 위젯 이벤트 폰트를 10sp로 조정하고, 날짜 숫자 박스 라운드를 절반 수준으로 줄였습니다.
- 위젯 이벤트 텍스트는 좌측 정렬을 유지했습니다.
- 위젯에서 내 비밀 일정은 자물쇠 없이 표시하고, 타인의 비밀 일정에만 자물쇠를 표시하도록 스냅샷/렌더링 정보를 보강했습니다.
- 위젯 이벤트 색상을 앱 월간 캘린더의 내 일정 파랑(`#3B82F6`), 타인 일정 초록(`#10B981`), 초대 대기 초록 테두리 스타일에 맞춰 조정했습니다.
- README/package 기준 버전을 `v.0.4.52`에서 `v.0.4.53`으로 업데이트했습니다.

### 작업 메모

- AppWidget은 웹 CSS를 직접 공유할 수 없으므로, 웹 snapshot에 `isMine`/`isPrivateLocked` 상태를 추가하고 native drawable 색상을 앱 이벤트 색상과 맞췄습니다.

## v.0.4.52 - 2026-06-08

### 업데이트 내용

- Android 월간 캘린더 위젯의 헤더와 캘린더 사이 여백을 약 10dp로 조정했습니다.
- 요일/날짜 숫자/이벤트 폰트를 각각 12sp로 맞췄습니다.
- 오늘 날짜 숫자 강조 박스와 날짜/이벤트 간격을 더 작게 조정했습니다.
- 이벤트 pill 내부 텍스트를 좌측 정렬로 변경했습니다.
- 월간 캘린더 위젯 그리드를 6행에서 5행으로 변경하고, 위젯 기본 크기를 4x5로 조정했습니다.
- README/package 기준 버전을 `v.0.4.51`에서 `v.0.4.52`로 업데이트했습니다.

### 작업 메모

- 5행 위젯 요구사항에 맞춰 Android 위젯 레이아웃과 웹→위젯 스냅샷의 visible date 범위를 35칸으로 통일했습니다.

## v.0.4.51 - 2026-06-08

### 업데이트 내용

- Android 월간 캘린더 위젯의 헤더/요일/날짜/이벤트 폰트 크기를 직전 버전 대비 절반 수준으로 줄여 전체 균형을 조정했습니다.
- README/package 기준 버전을 `v.0.4.50`에서 `v.0.4.51`로 업데이트했습니다.

### 작업 메모

- 보더, 오늘 날짜 작은 박스, 이벤트 rounded pill 구조는 유지하고 텍스트 크기만 조정했습니다.

## v.0.4.50 - 2026-06-08

### 업데이트 내용

- Android 월간 캘린더 위젯 레이아웃을 OC.zip `screens-calendar.jsx`의 월간 그리드 구조에 맞춰 날짜 숫자와 이벤트 pill을 분리한 구조로 재작성했습니다.
- `yyyy년 M월` 헤더 폰트를 크게 키우고, 요일/날짜/이벤트 텍스트 가독성을 강화했습니다.
- 요일 헤더와 6주 날짜 행에 셀 보더를 추가해 행/열 구분이 보이도록 조정했습니다.
- 오늘 날짜 강조를 전체 셀 배경이 아니라 날짜 숫자 주변의 작은 rounded 박스로 변경했습니다.
- 이벤트는 텍스트 길이와 관계없이 셀 내부 약 90% 폭의 rounded pill로 표시하고, 이벤트 사이 간격을 추가했습니다.
- README/package 기준 버전을 `v.0.4.49`에서 `v.0.4.50`으로 업데이트했습니다.

### 작업 메모

- Android AppWidget RemoteViews 제약 때문에 웹 CSS를 직접 재사용하지 않고, 동일한 시각 구조를 XML LinearLayout/TextView와 drawable 리소스로 재구성했습니다.

## v.0.4.49 - 2026-06-08

### 업데이트 내용

- Android 월간 캘린더 위젯의 일정 표시를 앱 월간 캘린더의 내 일정/타인 일정/초대 대기/숨김 일정 색상 체계에 맞춰 더 선명한 라벨 스타일로 개선했습니다.
- 위젯 상단의 `앱 월간 캘린더 동기화` 문구를 제거했습니다.
- Android 월간 캘린더 위젯 기본 크기를 4x4에서 4x6으로 변경하고, 가로 리사이즈로 5x6 구성을 사용할 수 있도록 조정했습니다.
- 숨김 일정의 웹 색상값(`#RRGGBBAA`)이 Android 위젯에서 잘못 파싱될 수 있는 문제를 보정했습니다.
- README/package 기준 버전을 `v.0.4.48`에서 `v.0.4.49`로 업데이트했습니다.

### 작업 메모

- AppWidget RemoteViews는 웹 CSS pill을 직접 렌더링할 수 없어, 같은 색상/상태 의미를 유지하면서 Android `Spannable` 배경/글자색 라벨로 표현했습니다.

## v.0.4.48 - 2026-06-08

### 업데이트 내용

- Android 월간 캘린더 위젯이 WebView 앱의 월간 캘린더 상태를 스냅샷으로 받아 일정/공휴일 요소를 함께 표시하도록 연결했습니다.
- 위젯 날짜 셀을 앱 월간 캘린더처럼 상단 날짜, 공휴일명, 최대 3개 일정, `+N` 더보기 형태로 표시하도록 조정했습니다.
- 공휴일과 주말 날짜 숫자가 위젯에서도 빨간색으로 표시되도록 보강했습니다.
- 앱이 아직 월간 캘린더 스냅샷을 보내지 않은 경우에도 `holiday.xml` 기반 공휴일을 위젯에 표시하도록 fallback을 추가했습니다.
- README/package 기준 버전을 `v.0.4.47`에서 `v.0.4.48`로 업데이트했습니다.

### 작업 메모

- Android AppWidget은 WebView DOM을 직접 렌더링할 수 없어서, 웹 월간 캘린더가 보이는 달/일정 정보를 JSON 스냅샷으로 Flutter 브리지에 전달하고 네이티브 위젯이 이를 다시 그리는 방식으로 구현했습니다.

## v.0.4.47 - 2026-06-04

### 업데이트 내용

- Android 홈 화면 위젯이 런처 위젯 목록에 안정적으로 노출되도록 receiver label/icon을 추가했습니다.
- 월간 캘린더 위젯 메타데이터에 4x4 셀 크기(`targetCellWidth`, `targetCellHeight`)와 resize 기준 크기를 명시했습니다.
- GitHub Actions release APK 빌드에서 package 버전 기반 `--build-number`를 함께 지정해 Android 설치 업데이트 인식을 보강했습니다.
- README/package 기준 버전을 `v.0.4.46`에서 `v.0.4.47`로 업데이트했습니다.

### 작업 메모

- 일부 Android 런처는 위젯 provider의 label/셀 크기 메타데이터가 부족하면 위젯 목록 표시가 불안정할 수 있어 메타데이터를 보강했습니다.

## v.0.4.46 - 2026-06-02

### 업데이트 내용

- Android 홈 화면에 이번 달 월간 캘린더 위젯을 추가했습니다.
- 위젯은 현재 월 제목, 요일, 날짜 6주 그리드, 오늘 강조 표시, 주말 색상을 표시합니다.
- 위젯을 누르면 OURCAL 앱이 열리도록 연결했습니다.
- 날짜/시간/시간대 변경 시 위젯이 다시 그려지도록 보강했습니다.
- README/package 기준 버전을 `v.0.4.45`에서 `v.0.4.46`으로 업데이트했습니다.

### 작업 메모

- Flutter analyze/test와 Android XML 파싱으로 1차 검증했습니다.
- Android Gradle 리소스/assemble 검증은 Docker 러너에서 Gradle 작업이 장시간 멈춰 최종 완료하지 못했습니다.

## v.0.4.45 - 2026-06-02

### 업데이트 내용

- 로그인 화면을 OURCAL 디자인 시스템에 맞춰 히어로/카드/소셜 버튼 형태로 재정리했습니다.
- 로그인 OAuth 진행 중에는 디자인 시스템 기반 로딩 오버레이를 표시하도록 변경했습니다.
- 로그인 콜백 처리 화면도 `OurcalSplash` 기반 로딩 화면으로 교체했습니다.
- Flutter iOS 프로젝트 scaffold를 추가해 iOS 빌드 기반을 마련했습니다.
- iOS 앱명, 앱 아이콘, 알림 background mode, APNs entitlement, Kakao/Chrome URL scheme query 설정을 추가했습니다.
- iOS FCM 토큰 발급 안정성을 위해 APNs 토큰 대기 후 FCM 토큰을 요청하도록 보강했습니다.
- README/app README에 iOS Firebase/Auth/Apple Developer 설정 중 Hamin이 해야 할 작업을 정리했습니다.
- README/package 기준 버전을 `v.0.4.44`에서 `v.0.4.45`로 업데이트했습니다.

### 작업 메모

- 웹은 lint/build로 검증했고, Flutter는 Docker Flutter 환경에서 `flutter analyze`와 `flutter test`로 검증했습니다.
- iOS 실제 빌드/서명은 macOS/Xcode 및 Apple Developer 설정이 필요해 로컬 Linux 환경에서는 수행하지 않았습니다.

## v.0.4.44 - 2026-06-01

### 업데이트 내용

- 날짜 클릭 하단 일정 팝업에서 공휴일 이벤트가 멤버 필터에 의해 제외되지 않도록 수정했습니다.
- 공휴일 이벤트는 하단 팝업에 표시하되 클릭할 수 없는 동작을 유지했습니다.
- 월간 캘린더에서 공휴일 날짜 숫자를 빨간색으로 표시하도록 수정했습니다.
- 보류 일정은 초록색 점선 보더와 빨간 점은 유지하되 배경색을 제거했습니다.
- README/package 기준 버전을 `v.0.4.43`에서 `v.0.4.44`로 업데이트했습니다.

### 작업 메모

- 웹 변경만 포함되어 APK 빌드는 수행하지 않았고, web lint/build로 검증했습니다.

## v.0.4.43 - 2026-06-01

### 업데이트 내용

- 보류 일정 색상을 주황색에서 그룹 일정과 같은 초록 계열로 변경했습니다.
- 보류 일정은 초록색 점선 테두리와 옅은 초록 배경으로 표시되도록 조정했습니다.
- 보류 일정 제목 오른쪽에 빨간 점을 별도 고정 영역으로 표시해 제목 말줄임표가 있어도 보이도록 했습니다.
- 날짜 하단 일정 팝업에서도 보류 일정 빨간 점 표시를 동일하게 적용했습니다.
- 공휴일은 날짜 하단 일정 팝업에 표시되지만 클릭되지 않는 동작을 유지했습니다.
- README/package 기준 버전을 `v.0.4.42`에서 `v.0.4.43`으로 업데이트했습니다.

### 작업 메모

- 웹 변경만 포함되어 APK 빌드는 수행하지 않았고, web lint/build로 검증했습니다.

## v.0.4.42 - 2026-06-01

### 업데이트 내용

- 공통 앱 다이얼로그 Provider 내부 fallback에서도 브라우저 기본 `window.alert/confirm`을 사용하지 않도록 제거했습니다.
- Provider mount 전 호출 가능성은 앱 다이얼로그 큐에 보관하도록 정리했습니다.
- README/package 기준 버전을 `v.0.4.41`에서 `v.0.4.42`로 업데이트했습니다.

### 작업 메모

- 웹 변경만 포함되어 APK 빌드는 수행하지 않았고, web lint/build로 검증했습니다.

## v.0.4.41 - 2026-06-01

### 업데이트 내용

- `OC.zip` 레퍼런스에 맞춰 일정 상세 팝업, 날짜 하단 팝업, 오늘 표시, 공휴일 이벤트 표시를 정리했습니다.
- 멤버 필터와 그룹 선택을 외부 클릭으로 닫히는 커스텀 드롭다운으로 개선했습니다.
- 알림 화면과 그룹 목록/상세 팝업을 현재 디자인 시스템에 맞춰 정리했습니다.
- 브라우저 기본 alert/confirm 사용처를 재사용 가능한 앱 공통 다이얼로그로 교체했습니다.
- README/package 기준 버전을 `v.0.4.40`에서 `v.0.4.41`로 업데이트했습니다.

### 작업 메모

- 웹 변경만 포함되어 APK 빌드는 수행하지 않았고, web lint/build로 검증했습니다.

## v.0.4.40 - 2026-06-01

### 업데이트 내용

- 롱프레스 범위 선택의 노란 배경 시작 시간을 0.7초에서 0.65초로 조정했습니다.
- 롱프레스 활성화 순간 현재 손가락 위치까지 바로 범위가 반영되도록 최신 터치/포인터 좌표를 추적합니다.
- 드래그 중 범위 업데이트가 더 자연스럽게 이어지도록 touch move 좌표를 직접 range 계산에 사용합니다.
- README/package 기준 버전을 `v.0.4.39`에서 `v.0.4.40`으로 업데이트했습니다.

### 작업 메모

- `screens-calendar.jsx`의 long-press + drag 흐름을 참고하되, FullCalendar/WebView 환경에 맞춰 활성화 시점의 실제 손가락 위치를 반영하도록 보강했습니다.

## v.0.4.39 - 2026-06-01

### 업데이트 내용

- 롱프레스 범위 선택의 노란 배경 시작 시간을 0.8초에서 0.7초로 조정했습니다.
- 햅틱 진동은 기존처럼 1.0초 기준을 유지합니다.
- README/package 기준 버전을 `v.0.4.38`에서 `v.0.4.39`로 업데이트했습니다.

### 작업 메모

- 월간 스와이프와의 충돌은 유지하면서 선택 시작 체감을 0.1초 더 빠르게 조정했습니다.

## v.0.4.38 - 2026-06-01

### 업데이트 내용

- 롱프레스 범위 선택 시작과 햅틱 진동 타이밍을 별도 상수로 분리했습니다.
- 노란 배경 선택 시작은 0.8초(`RANGE_SELECT_DELAY_MS`), 진동은 1.0초(`RANGE_HAPTIC_DELAY_MS`) 기준으로 조정했습니다.
- 스와이프/취소/릴리즈 시 선택 타이머와 진동 타이머가 함께 취소되도록 정리했습니다.
- README/package 기준 버전을 `v.0.4.37`에서 `v.0.4.38`로 업데이트했습니다.

### 작업 메모

- `navigator.vibrate()`는 WebView/OS가 비동기로 처리해 React 렌더링과 프레임 단위 동기화가 보장되지 않으므로 체감 타이밍 보정을 위해 분리했습니다.

## v.0.4.37 - 2026-06-01

### 업데이트 내용

- 모바일 롱프레스 범위 선택 시작 시간을 1.0초에서 0.8초로 조정했습니다.
- 햅틱 진동과 노란 선택 배경 시작도 동일한 0.8초 기준으로 동작합니다.
- README/package 기준 버전을 `v.0.4.36`에서 `v.0.4.37`로 업데이트했습니다.

### 작업 메모

- 월간 스와이프와 충돌을 줄이면서 체감 선택 시작을 0.2초 빠르게 조정했습니다.

## v.0.4.36 - 2026-06-01

### 업데이트 내용

- 모바일에서 롱프레스 범위 선택 타이머를 `pointerdown`이 아니라 즉시 발생하는 `touchstart` 기준으로 시작하도록 변경했습니다.
- 1초가 지난 순간 진동과 첫 노란 선택 배경이 바로 같이 표시되도록 `touchmove/touchend`에서도 직접 범위 업데이트와 CTA 확정을 처리합니다.
- pointer 이벤트에 밀려 범위 선택 시작/해제가 늦어지는 WebView 케이스를 줄였습니다.
- README/package 기준 버전을 `v.0.4.35`에서 `v.0.4.36`으로 업데이트했습니다.

### 작업 메모

- 터치 입력은 touch 이벤트를 우선 경로로 사용하고, mouse 입력은 기존 pointer 경로로 처리합니다.

## v.0.4.35 - 2026-06-01

### 업데이트 내용

- 롱프레스 범위 선택 지연 시간을 `RANGE_LONG_PRESS_DELAY_MS = 1000` 상수로 통일했습니다.
- 노란 범위 선택 시작과 햅틱 진동을 같은 1초 타이머 안에서 동시에 실행하도록 맞췄습니다.
- FullCalendar long press 관련 delay props도 같은 상수를 사용하도록 변경했습니다.
- README/package 기준 버전을 `v.0.4.34`에서 `v.0.4.35`로 업데이트했습니다.

### 작업 메모

- 진동은 브라우저/WebView 지원 여부에 따라 best-effort로 동작합니다.

## v.0.4.34 - 2026-06-01

### 업데이트 내용

- 오늘 날짜 표시를 숫자 보더가 아니라 월간 캘린더 일자 박스 전체 보더로 변경했습니다.
- FullCalendar `selectable`을 다시 활성화하고 기본 highlight는 투명 유지했습니다.
- 롱프레스 드래그 시작 기준을 1초로 늘려, 1초 전 수평 이동은 월간 스와이프(prev/next)로 먼저 처리되도록 조정했습니다.
- FullCalendar select 콜백도 바로 폼을 열지 않고 RangeCTA용 pending range를 만들도록 맞췄습니다.
- README/package 기준 버전을 `v.0.4.33`에서 `v.0.4.34`로 업데이트했습니다.

### 작업 메모

- 1초 이상 누른 뒤 드래그하면 범위 선택과 CTA가 뜨고, 1초 전에 좌우로 밀면 월 이동을 의도한 제스처로 처리합니다.

## v.0.4.33 - 2026-06-01

### 업데이트 내용

- 롱프레스 드래그 중 FullCalendar 기본 selection highlight가 주 시작점에 파란 배경으로 보이지 않도록 기본 selectable/highlight 동작을 비활성화했습니다.
- 오늘 날짜 표시를 배경 채움이 아닌 `theme.primary` 계열 보더 스타일로 변경했습니다.
- 롱프레스 드래그 해제 후 선택 범위 유지와 RangeCTA 표시 흐름은 유지했습니다.
- README/package 기준 버전을 `v.0.4.32`에서 `v.0.4.33`으로 업데이트했습니다.

### 작업 메모

- 날짜 범위 배경은 `screens-calendar.jsx` 참고안처럼 연한 노란색 계열로 유지하고, 오늘 날짜는 별도 primary 보더만 사용합니다.

## v.0.4.32 - 2026-06-01

### 업데이트 내용

- `OC.zip`의 `screens-calendar.jsx`를 참고해 월간 캘린더 롱프레스 드래그 릴리즈 후 pending range CTA가 뜨도록 변경했습니다.
- 롱프레스 드래그 중/릴리즈 후 선택 날짜 배경이 더 명확히 보이도록 range selected 스타일을 보강했습니다.
- 일간 하단 팝업의 `일정 추가` 버튼 디자인을 `DatePopup` 참고안처럼 50px 높이, plus 아이콘, primary 배경, 그림자 스타일로 변경했습니다.
- README/package 기준 버전을 `v.0.4.31`에서 `v.0.4.32`로 업데이트했습니다.

### 작업 메모

- 드래그 릴리즈 시 바로 생성 폼을 열지 않고, 선택 범위를 유지한 상태에서 CTA 확인 버튼을 누르면 종일 일정 생성 폼이 열립니다.

## v.0.4.31 - 2026-06-01

### 업데이트 내용

- 모바일 WebView에서 월간 캘린더 스와이프가 더 안정적으로 인식되도록 touch 이벤트 캡처 기반 제스처 처리를 추가했습니다.
- 왼쪽에서 오른쪽으로 스와이프하면 이전 달, 오른쪽에서 왼쪽으로 스와이프하면 다음 달로 이동하도록 동작을 명확히 보강했습니다.
- README/package 기준 버전을 `v.0.4.30`에서 `v.0.4.31`로 업데이트했습니다.

### 작업 메모

- FullCalendar 내부 포인터 처리와 충돌하지 않도록 touch 캡처에서 수평 이동 의도를 먼저 감지하고 롱프레스 선택 타이머를 취소합니다.

## v.0.4.30 - 2026-06-01

### 업데이트 내용

- 월간 캘린더 영역에서 좌/우 터치 스와이프로 이전/다음 달로 이동할 수 있게 했습니다.
- 스와이프 제스처가 날짜 클릭/일정 클릭/롱프레스 범위 선택으로 오인되지 않도록 이동 임계값과 클릭 억제를 추가했습니다.
- README/package 기준 버전을 `v.0.4.29`에서 `v.0.4.30`으로 업데이트했습니다.

### 작업 메모

- 좌로 밀면 다음 달, 우로 밀면 이전 달로 이동합니다.

## v.0.4.29 - 2026-06-01

### 업데이트 내용

- GitHub Actions의 release version 출력 스텝을 multiline shell로 변경해 command substitution quote 오류를 수정했습니다.
- README/package 기준 버전을 `v.0.4.28`에서 `v.0.4.29`로 업데이트했습니다.

### 작업 메모

- release artifact 예시는 현재 버전 기준 `ourcal-release-v.0.4.29`입니다.

## v.0.4.28 - 2026-06-01

### 업데이트 내용

- `web/public/logo_white_512.png` 기준으로 웹/PWA 아이콘과 Android launcher icon 리소스를 갱신했습니다.
- 앱에서 동일한 원본 로고를 추적할 수 있도록 `app/assets/logo_white_256.png`, `app/assets/logo_white_512.png`를 추가했습니다.
- GitHub Actions release APK artifact 이름을 README/package 기준 현재 버전이 포함된 `ourcal-release-v.x.y.z` 형식으로 변경했습니다.
- Flutter release 빌드의 `--build-name`도 현재 package 버전을 사용하도록 연결했습니다.
- README/package 기준 버전을 `v.0.4.27`에서 `v.0.4.28`로 업데이트했습니다.

### 작업 메모

- release artifact 예시는 현재 버전 기준 `ourcal-release-v.0.4.28`입니다.

## v.0.4.27 - 2026-06-01

### 업데이트 내용

- 공통 상단 콘텐츠 여백 변수 `--oc-content-top-padding`을 추가했습니다.
- 월간 캘린더 높이를 `100dvh - 상단 콘텐츠 여백 - 하단 네비 높이` 기준으로 계산하도록 변경했습니다.
- 일간 하단 팝업에 하단 네비게이션 높이만큼 padding을 추가해, 일정이 없는 상태의 `일정 추가` 버튼이 네비게이션바에 가려지지 않도록 수정했습니다.
- README/package 기준 버전을 `v.0.4.26`에서 `v.0.4.27`로 업데이트했습니다.

### 작업 메모

- 상단 safe-area + 5vh 여백은 레이아웃과 캘린더 높이 계산식이 같은 CSS 변수를 공유합니다.

## v.0.4.26 - 2026-06-01

### 업데이트 내용

- 웹 내부 상단 패딩을 `calc(env(safe-area-inset-top) + 5vh)`로 조정해 기존 safe-area 보정만큼 내부 화면 여백을 확보했습니다.
- viewport `viewport-fit=cover`를 추가해 WebView/PWA 환경에서 safe-area inset을 사용할 수 있게 했습니다.
- 날짜 클릭 하단 팝업에서 일정이 없는 경우 안내 문구 바로 아래 `일정 추가` 버튼을 노출하도록 변경했습니다.
- 일정이 있는 경우에는 기존처럼 팝업 하단에 전체 폭 `일정 추가` 버튼을 유지합니다.
- 모든 `<button>` 클릭에 0.5초 전역 debounce guard를 추가해 중복 클릭으로 인한 중복 실행을 방지합니다.
- README/package 기준 버전을 `v.0.4.25`에서 `v.0.4.26`으로 업데이트했습니다.

### 작업 메모

- 버튼 debounce는 개별 버튼 수정 대신 루트 클라이언트 가드에서 capture 단계로 처리해 신규 버튼에도 자동 적용됩니다.

## v.0.4.25 - 2026-06-01

### 업데이트 내용

- Flutter WebView shell의 상단 padding을 제거하고, 웹 `main` 내부 `pt-[5vh]`로 이동했습니다.
- 팝업 dim overlay가 상단 여백 영역까지 덮이도록 WebView는 전체 화면을 차지하게 유지합니다.
- 일정 추가 폼의 `하루 종일` 체크박스를 참석자 컴포넌트 내부가 아닌 참석자 영역과 시작 시간 영역 사이로 이동했습니다.
- 날짜 클릭 하단 팝업은 최대 높이 `58vh`는 유지하되, 일정 개수가 적으면 내용 높이에 맞춰 낮아지도록 고정 높이를 제거했습니다.
- 하단 팝업 일정 리스트에는 최대 높이를 두어 일정이 많을 때만 내부 스크롤되도록 조정했습니다.
- 일정이 없는 날짜에서도 DatePopup 하단의 `일정 추가` 버튼이 노출되도록 유지했습니다.
- README/package 기준 버전을 `v.0.4.24`에서 `v.0.4.25`로 업데이트했습니다.

### 작업 메모

- 상단 5vh 여백은 Flutter가 아니라 웹 레이아웃이 담당하므로, 모든 `fixed inset-0` dim 레이어가 해당 영역까지 커버합니다.

## v.0.4.24 - 2026-06-01

### 업데이트 내용

- 하단 네비게이션 전체 높이를 이전 기준의 `3/5`로 줄였습니다.
- 월간 캘린더 높이는 줄어든 `--oc-nav-height` 기준으로 다시 맞춰 FullCalendar와 네비게이션 사이 간격을 제거했습니다.
- 일정 추가 플로팅 버튼을 기존 대비 `9/10` 크기로 줄이고, 하단 네비 위에서 `2vh` 더 위로 배치했습니다.
- 날짜 클릭 바텀 팝업에서 일정이 없는 경우 `일정이 없습니다.`와 `이 날을 우리만의 시간으로 채워볼까요?` 안내 문구를 표시합니다.
- DatePopup 하단 일정 추가 버튼은 빈 상태에서도 유지되도록 정리했습니다.
- 일정 추가 폼에서 `하루 종일` 체크박스를 참석자 리스트 아래로 이동했습니다.
- `상세 일정 함께 보기` 체크박스를 OC.zip `screens-add-event.jsx`의 공개 설정 구조를 참고한 `공개 설정` 토글로 변경하고, 공개/비공개 상태별 안내 문구를 추가했습니다.
- README/package 기준 버전을 `v.0.4.23`에서 `v.0.4.24`로 업데이트했습니다.

### 작업 메모

- 공개 설정 토글은 기존 DB 필드(`is_hidden`)와 저장 로직을 유지하면서 UI만 공개/비공개 토글 형태로 변경했습니다.

## v.0.4.23 - 2026-05-31

### 업데이트 내용

- Flutter WebView shell의 상단 보정을 `7.5vh`에서 `5vh`로 줄였습니다.
- 하단 네비게이션 기준 높이를 `5vh`에서 `7.5vh`로 변경하고, 네비 영역을 `--oc-nav-height`와 동일한 고정 높이로 맞췄습니다.
- 월간 캘린더 높이를 `calc(100dvh - var(--oc-nav-height) + 1px)`로 조정해 하단 네비게이션과 FullCalendar 사이의 잔여 간격을 제거했습니다.
- 하단 네비게이션은 border/shadow 없이 유지했습니다.
- 친구/그룹/설정 화면의 좌우 패딩을 `5vw`로 변경했습니다.
- 친구 탭의 친구 목록 액션 버튼 라운딩을 제거해 리스트 항목 라운딩이 남지 않도록 정리했습니다.
- README/package 기준 버전을 `v.0.4.22`에서 `v.0.4.23`으로 업데이트했습니다.

### 작업 메모

- 상단/하단 비율 보정은 기기 높이에 따라 같은 비율로 유지되도록 vh 기반으로 관리합니다.

## v.0.4.22 - 2026-05-31

### 업데이트 내용

- Flutter WebView shell의 상단 보정을 고정 10px에서 화면 높이 기준 `7.5vh` 비율로 변경했습니다.
- 하단 네비게이션의 border/shadow를 제거하고, 하단 padding을 `5vh`로 늘렸습니다.
- 공통 `--oc-nav-height` 값을 추가해 월간 캘린더 높이를 하단 네비게이션 높이에 맞추고 캘린더-네비 사이 빈 공간을 줄였습니다.
- 친구/그룹/설정 화면에 OC.zip `screens-friends-settings.jsx` 기준의 외곽 여백을 추가해 요소가 화면 끝에 붙지 않도록 조정했습니다.
- 친구 목록 리스트의 외곽 라운딩을 제거했습니다.
- 월간 캘린더 날짜 클릭 하단 팝업의 일간 일정 목록을 OC.zip `DatePopup`처럼 행 리스트 형태로 바꾸고, 일정 카드 라운딩/섀도우를 제거했습니다.
- README/package 기준 버전을 `v.0.4.21`에서 `v.0.4.22`로 업데이트했습니다.

### 작업 메모

- 웹 safe-area padding은 계속 제거하고, 기기별 시스템 inset과 상단 비율 보정은 Flutter shell에서 담당합니다.

## v.0.4.21 - 2026-05-31

### 업데이트 내용

- Flutter WebView shell의 상단 시스템 safe-area 보정을 다시 켜고, 추가 상단 여백 10px을 적용했습니다.
- 하단 네비게이션 버튼 세로 패딩을 5px 높아지도록 조정했습니다.
- 월간 캘린더 화면 높이를 조정해 캘린더 하단과 네비게이션바 사이 공백을 줄였습니다.
- OC.zip `icons.jsx`의 Heroicons 기반 아이콘을 React 컴포넌트로 이식하고 하단 네비, 캘린더 헤더, 알림 버튼에 적용했습니다.
- OC.zip `screens-core.jsx`의 Splash 디자인을 참고한 초기 진입 로딩 화면을 추가하고, 월간 캘린더 로딩도 같은 톤으로 통일했습니다.
- README/package 기준 버전을 `v.0.4.20`에서 `v.0.4.21`로 업데이트했습니다.

### 작업 메모

- 웹 하단 네비의 safe-area padding 제거는 유지하고, 실제 시스템 inset 보정은 Flutter shell에서 담당합니다.

## v.0.4.20 - 2026-05-31

### 업데이트 내용

- Flutter WebView shell을 `SafeArea(bottom: false)` 방식에서 `Scaffold(extendBody: true)`와 `MediaQuery.paddingOf(context).bottom` 기반 edge-to-edge 보정 방식으로 변경했습니다.
- README/package 기준 버전을 `v.0.4.19`에서 `v.0.4.20`으로 업데이트했습니다.

### 작업 메모

- 웹 하단 네비 자체의 safe-area padding은 제거된 상태를 유지하고, 실제 기기 시스템 제스처 영역 보정은 Flutter shell에서 담당합니다.

## v.0.4.19 - 2026-05-31

### 업데이트 내용

- Flutter WebView에서 하단 네비게이션 safe-area padding이 48px로 잡히는 문제를 해결하기 위해 `pb-[env(safe-area-inset-bottom)]`를 제거했습니다.
- 월간 캘린더 상단 그룹 선택 드롭다운을 컨텐츠 너비 기준으로 줄이고, 멤버 드롭다운 왼쪽에 12px 간격을 둔 오른쪽 정렬 배치로 변경했습니다.
- 월간 캘린더의 외곽 라운딩, 보더, 쉐도우 느낌을 제거하고 좌우 공백 없이 네모 칸 형태로 이어지게 조정했습니다.
- 달력 날짜 숫자를 각 칸의 가운데 위로 정렬했습니다.
- 월간 캘린더 요일 헤더를 `일 월 화 수 목 금 토` 한글 표기로 변경했습니다.
- README/package 기준 버전을 `v.0.4.18`에서 `v.0.4.19`로 업데이트했습니다.

### 작업 메모

- 하단 공백의 직접 원인은 BottomNavigation 내부의 safe-area padding이었습니다.

## v.0.4.18 - 2026-05-31

### 업데이트 내용

- 하단 웹 네비게이션 높이/아이콘 크기 변경을 롤백하고, Flutter WebView의 하단 SafeArea padding을 제거해 앱 껍데기 하단 공백을 줄였습니다.
- 전역 고정 알림 버튼을 제거하고 월간 캘린더 헤더 안의 버튼으로 이동했습니다.
- FullCalendar 기본 headerToolbar를 끄고, 월간 캘린더 상단에 `< 2026. 5 >` 형태의 커스텀 월 이동 버튼을 추가했습니다.
- 월간 캘린더 상단 컨트롤을 OC.zip `screens-calendar.jsx` 기준으로 월 이동/그룹보기 토글/알림/그룹선택/멤버 드롭다운 배치로 재구성했습니다.
- OC.zip `screens-friends-settings.jsx`의 GroupTab/SettingsRow 스타일을 참고해 그룹 탭과 설정 페이지 UI를 최신화했습니다.
- README/package 기준 버전을 `v.0.4.17`에서 `v.0.4.18`로 업데이트했습니다.

### 작업 메모

- 최신 dev의 직전 커밋은 OC.zip 갱신 merge였고, 코드 충돌은 없었습니다.
- 기존 `v.0.4.17`의 웹 네비게이션 축소는 원인과 다르다고 판단해 원복했습니다.

## v.0.4.17 - 2026-05-31

### 업데이트 내용

- 모바일 앱 WebView에서 하단 네비게이션이 과하게 높아 보이지 않도록 네비게이션 패딩, 아이콘 크기, 라벨 크기, 본문 하단 여백을 줄였습니다.
- 로그인 화면에 Google OAuth 로그인 버튼을 추가했습니다.
- README/package 기준 버전을 `v.0.4.16`에서 `v.0.4.17`로 업데이트했습니다.

### 작업 메모

- Google OAuth는 기존 Supabase OAuth 콜백(`/auth/callback`) 흐름을 그대로 사용합니다.
- Android 로컬 빌드 산출물인 `app/android/.kotlin/`을 gitignore에 추가했습니다.

## v.0.4.16 - 2026-05-31

### 업데이트 내용

- notifications 생성과 FCM 발송을 함께 처리하는 서버 API `/api/notifications`를 추가했습니다.
- 친구 요청(`friend_request`)과 일정 초대(`event_invite`) 알림 생성 흐름을 FCM 푸시 발송 흐름에 연결했습니다.
- 그룹 초대 시 초대 대상 프로필에 `group_request` notifications row를 생성하고 FCM 푸시를 발송하도록 추가했습니다.
- FCM payload의 임시 진입점은 모두 `/notifications`로 통일했습니다.
- GitHub Actions APK 빌드가 FCM 활성 빌드(`OURCAL_ENABLE_FCM=true`)를 수행하도록 변경하고 Firebase Android 설정 복원 단계를 추가했습니다.
- Android Firebase Gradle 플러그인 설정과 `google-services.json` 위치를 정리했습니다.
- README/package 기준 버전을 `v.0.4.15`에서 `v.0.4.16`으로 업데이트했습니다.

### 작업 메모

- Vercel에는 `SUPABASE_SERVICE_ROLE_KEY`, `FIREBASE_PROJECT_ID`, `FIREBASE_CLIENT_EMAIL`, `FIREBASE_PRIVATE_KEY` 환경변수를 등록해야 실제 FCM 발송이 동작합니다.
- 운영 Supabase에는 `web/supabase/create_notifications.sql`을 다시 실행해 `group_request` type 제약조건을 반영해야 합니다.

## v.0.4.15 - 2026-05-31

### 업데이트 내용

- Flutter 앱에 FCM 토큰 발급/갱신을 WebView로 전달하는 `FcmBridge` 기본 구조를 추가했습니다.
- 웹 앱에 WebView가 전달한 FCM 토큰을 로그인된 프로필 기준 `push_tokens` 테이블에 upsert하는 등록 컴포넌트를 추가했습니다.
- Android 13+ 알림 권한 선언과 Firebase/FCM Flutter 의존성을 추가했습니다.
- Supabase용 `web/supabase/create_push_tokens.sql`을 추가해 토큰 저장 테이블, 인덱스, RLS 정책을 생성할 수 있게 했습니다.
- Firebase 설정 파일과 서비스 계정 키가 git에 들어가지 않도록 `.gitignore`를 보강했습니다.
- README/package 기준 버전을 `v.0.4.14`에서 `v.0.4.15`로 업데이트했습니다.

### 작업 메모

- Firebase 앱 등록 전 기존 빌드가 깨지지 않도록 FCM 초기화는 `--dart-define=OURCAL_ENABLE_FCM=true`일 때만 동작합니다.
- 현재 레포에는 Android 프로젝트만 있고 iOS 프로젝트 폴더는 아직 없습니다. iOS 배포가 필요하면 iOS 플랫폼을 생성한 뒤 Bundle ID를 Firebase에 등록해야 합니다.

## v.0.4.14 - 2026-05-28

### 업데이트 내용

- `OC.zip` 매핑 기준에 맞춰 `screens-friends-settings.jsx`의 친구 화면 카드/멤버 리스트/초대코드 UI를 `FriendsPanel`에 반영했습니다.
- `screens-calendar.jsx`의 모바일 bottom sheet 흐름을 월간 캘린더 날짜 팝업에 더 가깝게 반영했습니다.
- `screens-add-event.jsx`의 full-height bottom sheet, 입력 필드, 공개 설정 카드, 참석자 초대 모달 스타일을 일정 추가/수정 폼에 반영했습니다.
- README/package 기준 버전을 `v.0.4.13`에서 `v.0.4.14`로 업데이트했습니다.

### 작업 메모

- 기존 DB/이벤트 로직은 유지하고 JSX 시안의 레이아웃·색상·간격·시트 패턴을 OURCAL 컴포넌트에 맞춰 이식했습니다.

## v.0.4.13 - 2026-05-28

### 업데이트 내용

- `OC.zip` 디자인 시안의 네이비 팔레트와 모바일 앱형 레이아웃을 OURCAL 웹 UI에 반영했습니다.
- 전역 디자인 토큰, FullCalendar 월간 그리드, 하단 탭바, 그룹 선택/멤버 필터, 일정 팝업/폼의 라운드·그림자·색상 스타일을 통일했습니다.
- README/package 기준 버전을 `v.0.4.12`에서 `v.0.4.13`으로 업데이트했습니다.

### 작업 메모

- `OC.zip` 내부의 `theme.js`, `screens-calendar.jsx`, `screens-core.jsx`, `screens-add-event.jsx`를 기준으로 적용했습니다.
- `npm run web:lint`, `tsc --noEmit`, `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` 더미 값을 넣은 `npm run web:build`로 검증했습니다.

## v.0.4.12 - 2026-05-28

### 업데이트 내용

- 캘린더 로딩 컴포넌트를 파스텔 그라데이션 카드와 달력/발바닥 애니메이션으로 귀엽게 개선했습니다.
- 로딩 상태에 `role="status"`와 `aria-live="polite"`를 추가해 보조기기에서도 상태 메시지를 자연스럽게 전달하도록 했습니다.
- README/package 기준 버전을 `v.0.4.11`에서 `v.0.4.12`로 업데이트했습니다.

### 작업 메모

- UI 전용 변경이라 `npm run web:lint`로 정적 검사를 검증했습니다.

## v.0.4.11 - 2026-05-19

### 업데이트 내용

- 월간 캘린더 마스터토글을 좌/우 이모지 개별 버튼 방식에서 스위치 전체 클릭 방식으로 변경했습니다.
- 스위치를 클릭하면 현재 상태의 반대 상태로 이동하고, 같은 동작에서 멤버토글도 전체 ON으로 초기화됩니다.
- README/package 기준 버전을 `v.0.4.10`에서 `v.0.4.11`로 업데이트했습니다.

### 작업 메모

- 접근성은 `role="switch"`와 `aria-checked`로 현재 👥 상태 여부를 표현합니다.

## v.0.4.10 - 2026-05-19

### 업데이트 내용

- 마스터토글 클릭 시 `flushSync`로 토글 이동과 멤버토글 전체 ON 초기화를 같은 렌더 동작 안에서 처리하도록 보강했습니다.
- 알림 페이지 진입 시 알림 목록 query가 항상 DB에서 최신 `is_read` 값을 다시 가져오도록 `staleTime: 0`, `gcTime: 0`, `refetchOnMount: always`를 적용했습니다.
- 하단 네비게이션에서 친구/그룹 탭을 👥 단일 탭으로 통합하고, 그룹 페이지 상단 토글로 친구/그룹 화면을 전환하도록 변경했습니다.
- 친구 추가 검색을 닉네임 검색에서 profiles.id 앞 8자리 초대코드 검색으로 변경했습니다.
- 초대코드 검색 RPC `find_profile_by_invite_code` SQL을 추가했습니다.
- 친구 추가 팝업 높이를 기존 70vh에서 47vh로 줄였습니다.
- 설정 페이지 상단에 내 닉네임과 초대코드를 표시하고, 📋 버튼으로 초대코드를 클립보드에 복사하도록 추가했습니다.
- README/package 기준 버전을 `v.0.4.9`에서 `v.0.4.10`으로 업데이트했습니다.

### 작업 메모

- Supabase에 `web/supabase/find_profile_by_invite_code.sql`을 적용해야 초대코드 검색 RPC가 동작합니다.

## v.0.4.9 - 2026-05-19

### 업데이트 내용

- 알림 페이지는 진입 시점에 조회한 `is_read` 값으로 빨간 점을 표시하고, 같은 진입 중 읽음 처리 결과로 리스트를 즉시 갱신하지 않도록 수정했습니다.
- 마스터토글 클릭 시 토글 이동과 멤버토글 전체 ON 초기화가 한 동작에서 함께 처리되도록 `handleMasterFilterChange`로 묶었습니다.
- README/package 기준 버전을 `v.0.4.8`에서 `v.0.4.9`로 업데이트했습니다.

### 작업 메모

- 알림 페이지는 DB 읽음 처리 후 알림 버튼 unread query만 invalidate합니다. 리스트 빨간 점은 다음 진입/재조회 시 DB의 최신 `is_read` 값을 기준으로 사라집니다.

## v.0.4.8 - 2026-05-19

### 업데이트 내용

- 월간 캘린더 멤버토글이 모두 꺼진 상태에서 그룹을 변경하거나 마스터토글을 조작하면 멤버토글이 전체 ON 상태로 초기화되도록 수정했습니다.
- 월간 캘린더 상단 제목을 `May 2026` 형식에서 `yyyy mm` 숫자 형식으로 변경했습니다.
- 알림 페이지 진입 후 `is_read`를 true로 갱신하면 알림 리스트 내부의 읽지 않음 빨간 점도 즉시 사라지도록 캐시를 동기화했습니다.
- README/package 기준 버전을 `v.0.4.7`에서 `v.0.4.8`로 업데이트했습니다.

### 작업 메모

- 알림 리스트는 DB 업데이트 성공 후 React Query 리스트 캐시를 읽음 상태로 갱신하고, 알림 버튼 unread query는 기존처럼 invalidate합니다.

## v.0.4.7 - 2026-05-19

### 업데이트 내용

- 날짜별 일정 목록 팝업의 삭제 버튼이 실제 클릭한 일정 ID로 삭제되도록 수정했습니다.
- 일정 삭제를 `comments`/`events_invite`/`events` 순서로 처리하는 `delete_event_cascade` RPC SQL을 추가하고, 클라이언트는 RPC 우선 + 미배포 fallback으로 동작하게 했습니다.
- 월간 캘린더 그룹 선택 드롭다운 아래에 마스터토글(👤/👥)을 추가했습니다. 기본값은 👥이며, 👤 선택 시 내 일정만 표시합니다.
- 👥 옆 멤버 드롭다운에 나와 그룹 멤버 목록 및 멤버토글을 추가했습니다. 마스터토글이 👥일 때 멤버별 표시/숨김을 휘발성으로 제어합니다.
- 마스터토글이 👤이면 멤버 드롭다운/토글이 비활성화되고 내 일정만 표시됩니다.
- README/package 기준 버전을 `v.0.4.6`에서 `v.0.4.7`로 업데이트했습니다.

### 작업 메모

- 멤버 필터 상태는 React state만 사용하므로 새로고침/재방문 시 저장되지 않습니다.
- Supabase에 `web/supabase/delete_event_cascade.sql`을 적용하면 삭제가 DB 트랜잭션으로 처리됩니다.

## v.0.4.6 - 2026-05-18

### 업데이트 내용

- 월간 캘린더에서 타인의 공개 일정 제목 옆에도 소유자 닉네임을 표시하도록 변경했습니다.
- 같은 표시 우선순위와 시작 시간의 일정은 내 일정이 먼저 오고, 이후 일정 제목 오름차순으로 정렬되도록 보강했습니다.
- 날짜별 일정 목록 팝업에서 타인의 비공개 일정은 하단 닉네임을 중복 표시하지 않도록 수정했습니다.
- 날짜별 일정 목록 팝업에 내 일정 삭제 버튼을 추가하고, 기존 일정 수정 화면의 삭제 흐름과 동일하게 댓글/초대/일정을 삭제하도록 연결했습니다.
- 날짜별 일정 목록 팝업에서 내 일정과 타인의 공개 일정 오른쪽에 댓글 수를 `📝N` 형태로 표시하도록 추가했습니다.
- README/package 기준 버전을 `v.0.4.5`에서 `v.0.4.6`으로 업데이트했습니다.

### 작업 메모

- 댓글 수는 현재 월간 캘린더 조회 범위의 일정 ID를 기준으로 `comments.events_id`를 집계해 표시합니다.

## v.0.4.5 - 2026-05-16

### 업데이트 내용

- GitHub Actions에서 `dev` 브랜치 기준 Flutter release APK를 빌드하고 artifact로 업로드하는 `Build Flutter APK` 워크플로를 추가했습니다.
- Android release signing을 `android/key.properties` 기반으로 구성하고, CI에서는 GitHub Secrets의 keystore/base64 값을 복원해 고정 서명키로 빌드하도록 했습니다.
- Flutter WebView에서 카카오 OAuth 중 발생할 수 있는 `kakaokompassauth`, `kakaolink` 등 외부 스킴을 Android 외부 앱으로 넘기도록 처리했습니다.
- README에 GitHub Actions APK 빌드, Android signing secrets, Kakao Developers 키 해시 등록 안내를 추가했습니다.
- README/package 기준 버전을 `v.0.4.4`에서 `v.0.4.5`로 업데이트했습니다.

### 작업 메모

- 로컬 환경에는 Flutter CLI가 없어 Docker Flutter SDK로 `flutter analyze`, `flutter test`를 검증했습니다.
- GitHub Actions의 release APK 빌드는 Android signing secrets가 없으면 의도적으로 실패하도록 구성했습니다.

## v.0.4.4 - 2026-05-14

### 업데이트 내용

- 알림 버튼의 unread 상태를 페이지 이동 시마다 DB에서 다시 조회하도록 변경했습니다.
- React Query 캐시가 이전 페이지의 unread 값을 잠시 재사용하지 않도록 `staleTime: 0`, `gcTime: 0`, `refetchOnMount: always`를 적용했습니다.
- 알림 페이지에서 unread를 읽음 처리한 뒤 알림 unread query를 invalidate하도록 했습니다.
- 알림 버튼 크기를 기존 대비 약 2/3로 줄였습니다.
- 알림 페이지(`/notifications`)에서는 오른쪽 상단 알림 버튼이 보이지 않도록 숨겼습니다.
- README/package 기준 버전을 `v.0.4.3`에서 `v.0.4.4`로 업데이트했습니다.

### 작업 메모

- Hamin 요청대로 무거운 `next build` 검증은 실행하지 않았고, 가벼운 `npm run web:lint`만 실행했습니다.

## v.0.4.3 - 2026-05-14

### 업데이트 내용

- 오른쪽 상단에 알림 버튼(🔔)을 추가하고, 읽지 않은 알림이 있으면 빨간 점을 표시하도록 했습니다.
- `web/app/notifications/page.tsx` 알림 페이지를 추가했습니다. 뒤로가기 버튼과 type/title/message 형태의 알림 리스트를 표시합니다.
- 알림 페이지 진입 시 현재 프로필의 읽지 않은 알림을 읽음 처리하도록 했습니다.
- 일정 초대 저장 후 새로 초대된 참석자에게 `event_invite` 알림을 생성하도록 했습니다.
- 친구 요청 시 `friend_request` 알림을 생성하는 기본 흐름을 추가했습니다.
- Supabase 알림 테이블/RLS 기준 파일 `web/supabase/create_notifications.sql`을 추가했습니다.
- README/package 기준 버전을 `v.0.4.2`에서 `v.0.4.3`으로 업데이트했습니다.

### 작업 메모

- 알림 insert는 원본 작업(일정 저장/친구 요청)을 막지 않도록 best-effort로 처리합니다.
- 일정 수정 시 기존 초대자는 중복 알림을 보내지 않고 새로 추가된 초대자에게만 알림을 보냅니다.

## v.0.4.2 - 2026-05-14

### 업데이트 내용

- Vercel이 monorepo 루트에서 기존 Next.js 앱을 찾지 못해 랜딩 URL이 404가 나는 문제를 해결하기 위해 루트 `vercel.json`을 추가했습니다.
- Vercel install/build/dev/output 경로를 `web/` 기준으로 명시했습니다.
- Flutter WebView shell의 기본 URL을 `https://gongcal.vercel.app`로 변경했습니다.
- README/package 기준 버전을 `v.0.4.1`에서 `v.0.4.2`로 업데이트했습니다.

### 작업 메모

- Vercel Project Settings에서 Root Directory를 따로 `web`으로 바꾸지 않아도 repo 설정만으로 `web/.next` 결과를 배포하도록 맞췄습니다.

## v.0.4.1 - 2026-05-13

### 업데이트 내용

- 레포를 `web/`(Next.js 웹 앱)과 `app/`(Flutter 모바일 앱) 구조로 분리했습니다.
- Flutter 앱 껍데기를 추가하고, 현재는 OURCAL 웹 화면을 WebView로 표시하도록 구현했습니다.
- Android 인터넷 권한과 WebView 의존성(`webview_flutter`)을 추가했습니다.
- 루트 `package.json`은 monorepo용 위임 스크립트(`web:dev`, `web:build`, `web:lint`, `app:build:apk`)를 제공합니다.
- Flutter 앱의 웹 주소는 `--dart-define=OURCAL_WEB_URL=...`로 바꿀 수 있게 했습니다.
- README/package 기준 버전을 큰 구조 변경에 맞춰 `v.0.3.26`에서 `v.0.4.1`로 업데이트했습니다.

### 작업 메모

- Docker Flutter SDK로 `flutter analyze`, `flutter test`, 웹 lint/typecheck는 통과했습니다.
- 현재 실행 환경 메모리가 낮아 Gradle APK 빌드는 `SIGKILL`로 중단되어 APK 산출물 생성은 아직 완료하지 못했습니다.

## v.0.3.26 - 2026-05-13

### 업데이트 내용

- 일정 추가/수정과 일정 상세 화면의 참석자 목록 높이를 20vh에서 15vh로 줄였습니다.
- 일정 상세 화면 댓글 목록 높이를 25vh에서 20vh로 줄였습니다.
- `상세 일정 함께 보기`의 `?` 안내를 alert 대신 버튼 위에 표시되는 2초짜리 툴팁으로 변경했습니다.
- README/package 기준 버전을 `v.0.3.25`에서 소규모 패치 버전 `v.0.3.26`으로 업데이트했습니다.

### 작업 메모

- 툴팁은 `?` 버튼 클릭 시 위쪽에 표시되고 2초 후 자동으로 사라집니다.

## v.0.3.25 - 2026-05-13

### 업데이트 내용

- 일정 추가/수정 폼의 세부일정 입력을 textarea에서 한 줄 input으로 변경해 공간 사용을 줄였습니다.
- 일정 추가/수정 폼의 참석자 컴포넌트를 20vh 고정 높이로 변경하고, 참석자가 많으면 내부에서 스크롤되도록 조정했습니다.
- `상세 일정 함께 보기`의 `?` 안내를 체크박스 label 밖의 버튼으로 분리하고 클릭 시 안내 문구가 뜨도록 변경했습니다.
- 일정 상세 화면의 세부 내용과 댓글 사이에 읽기 전용 참석자 명단을 추가했습니다.
- 상세 화면 참석자 명단은 20vh, 댓글 목록은 25vh 고정 높이와 내부 스크롤로 처리했습니다.
- README/package 기준 버전을 `v.0.3.24`에서 소규모 패치 버전 `v.0.3.25`로 업데이트했습니다.

### 작업 메모

- 상세 화면 참석자 명단은 기존 `get_event_invite_attendees` RPC/직접 조회 fallback을 재사용하며 수정 버튼 없이 상태만 표시합니다.

## v.0.3.24 - 2026-05-13

### 업데이트 내용

- 월간 캘린더에서 토요일/일요일 날짜 숫자를 빨간색으로 표시하도록 변경했습니다.
- `public/holiday.xml`을 읽어 `locdate`에 해당하는 공휴일 날짜 숫자를 빨간색으로 표시하도록 추가했습니다.
- 공휴일 날짜에는 날짜 숫자 왼쪽에 `dateName`을 작은 빨간색 글씨로 표시합니다.
- README/package 기준 버전을 `v.0.3.23`에서 소규모 패치 버전 `v.0.3.24`로 업데이트했습니다.

### 작업 메모

- 로컬 `dev`에는 아직 `public/holiday.xml`이 보이지 않아, 파일이 없으면 조용히 넘어가고 파일이 배포되면 자동 적용되도록 구현했습니다. XML은 `item > locdate`, `item > dateName` 구조를 기준으로 파싱합니다.

## v.0.3.23 - 2026-05-13

### 업데이트 내용

- 로컬 개발 화면 확인을 위해 `next.config.ts`에 `localhost:3000`, `127.0.0.1:3000` 개발 허용 origin을 명시했습니다.
- `.env.example`을 추가해 로컬 `.env.local` 생성 기준을 제공했습니다.
- README에 Supabase Auth Redirect URLs에 `http://localhost:3000/auth/callback`을 추가해야 한다는 로컬 로그인 설정 안내를 보강했습니다.
- README/package 기준 버전을 `v.0.3.22`에서 소규모 패치 버전 `v.0.3.23`으로 업데이트했습니다.

### 작업 메모

- 실제 Supabase URL/Anon Key는 비밀값 성격이 있으므로 repo에는 예시 파일만 추가했습니다. 로컬에서는 `.env.example`을 `.env.local`로 복사한 뒤 실제 값을 입력해야 합니다.

## v.0.3.22 - 2026-05-13

### 업데이트 내용

- 월간 캘린더 전체 높이를 기존 대비 1/6 늘렸습니다.
- FullCalendar 월간 6주 행 높이를 `calc(100% / 6)` 기준으로 고정해 주마다 높이가 달라지지 않도록 조정했습니다.
- 캘린더 제목 영역과 캘린더 본문 사이 여백을 조금 늘렸습니다.
- README/package 기준 버전을 `v.0.3.21`에서 소규모 패치 버전 `v.0.3.22`로 업데이트했습니다.

### 작업 메모

- 기존 viewport 기반 row height 변수 대신 실제 캘린더 컨테이너 높이를 6등분하도록 CSS를 단순화했습니다.

## v.0.3.21 - 2026-05-13

### 업데이트 내용

- 초대받은 일정의 참석하기/참석거절/참석취소 처리를 `respond_event_invite` RPC 우선 사용으로 변경했습니다.
- 참석하기는 `events_invite.is_agree=true`로 업데이트하고, 참석거절/참석취소는 해당 `events_invite` row를 삭제하도록 명확히 분리했습니다.
- 응답 완료 후 상세/목록 팝업을 닫고 월간 캘린더를 다시 조회해 수락한 초대 일정이 파란색으로 갱신되도록 했습니다.
- README/package 기준 버전을 `v.0.3.20`에서 소규모 패치 버전 `v.0.3.21`로 업데이트했습니다.

### 작업 메모

- 운영 Supabase에 `supabase/respond_event_invite.sql`을 적용하면 RLS 영향을 받지 않고 초대 응답 처리가 안정적으로 동작합니다. RPC가 없으면 기존 직접 update/delete fallback을 시도합니다.

## v.0.3.20 - 2026-05-13

### 업데이트 내용

- 월간 캘린더의 초대받은 일정 조회를 `get_my_invited_events` RPC 우선 사용으로 변경했습니다.
- `events_invite.profile_id = 현재 profiles.id`인 초대 row와 연결된 `events` 데이터를 RLS 영향 없이 가져오도록 Supabase SQL을 추가했습니다.
- RPC가 아직 DB에 없으면 기존 직접 조회 fallback을 유지합니다.
- README/package 기준 버전을 `v.0.3.19`에서 소규모 패치 버전 `v.0.3.20`으로 업데이트했습니다.

### 작업 메모

- 운영 Supabase에 `supabase/get_my_invited_events.sql`을 적용해야 월간 캘린더에서 초대받은 일정이 안정적으로 표시됩니다.

## v.0.3.19 - 2026-05-13

### 업데이트 내용

- 일정 수정 팝업의 참석자 목록 조회를 `events_invite.event_id = 현재 일정 id` 기준 RPC로 명확히 분리했습니다.
- `get_event_invite_attendees` Supabase RPC SQL을 추가해 이벤트 소유자가 초대 참석자 row를 RLS 영향 없이 조회할 수 있도록 했습니다.
- RPC가 DB에 없을 때도 기존 `events_invite` 직접 조회 fallback을 수행하도록 했습니다.
- README/package 기준 버전을 `v.0.3.18`에서 소규모 패치 버전 `v.0.3.19`로 업데이트했습니다.

### 작업 메모

- 운영 Supabase에 `supabase/get_event_invite_attendees.sql`을 적용해야 일정 수정 팝업에서 저장된 `events_invite` 참석자 목록이 안정적으로 표시됩니다.

## v.0.3.18 - 2026-05-13

### 업데이트 내용

- Vercel 빌드 오류 원인인 `middleware.ts`와 `proxy.ts` 동시 사용을 해소하기 위해 `middleware.ts`를 제거하고 `proxy.ts`의 `proxy` export와 matcher config만 사용하도록 정리했습니다.
- 일정 추가/수정 화면의 `상세 일정 함께 보기` 체크박스 옆에 `?` 도움말 아이콘과 툴팁을 추가했습니다.
- 참석자 리스트 로딩을 `events_invite.event_id` 기준으로 명확히 재구성하고, 수락 여부와 무관하게 저장된 초대 row를 모두 표시하도록 보강했습니다.
- README/package 기준 버전을 `v.0.3.17`에서 소규모 패치 버전 `v.0.3.18`로 업데이트했습니다.

### 작업 메모

- 툴팁 문구: `구성원에게는 시간만 노출되고 '🔒'로 표시됩니다.`
- 초대받은 일정은 기존 정책대로 `events_invite.profile_id`가 현재 프로필이면 원본 `events.user_id`와 상관없이 내 일정처럼 병합합니다.

## v.0.3.17 - 2026-05-13

### 업데이트 내용

- `middleware.ts`의 인증 로직을 `proxy.ts`로 분리하여 관심사를 분리하였습니다.
- `proxy.ts`에서 서버 사이드 인증 상태를 검사하여, 로그인 세션이 있는 경우 `/` 및 `/login` 접근 시 `/calendar`로 자동 리다이렉트합니다.
- 비로그인 상태에서 보호된 경로(`/calendar`, `/day`, `/friends`, `/groups`, `/settings`, `/setup-profile`) 접근 시 `/login`으로 리다이렉트합니다.
- `middleware.ts`는 `proxy.ts`를 import하는 얇은 래퍼로 단순화하였습니다.
- `app/page.tsx`의 클라이언트 세션 체크 로직을 단순화 (중복 `onAuthStateChange` 제거, `getUser()` 단일 호출로 통합).

### 작업 메모

- `getUser()`는 JWT를 서버에서 검증하므로 `getSession()`보다 보안적으로 안전합니다.
- 미들웨어는 서버 사이드 리다이렉트를 담당하고, `app/page.tsx`는 클라이언트 사이드 내비게이션 폴백 역할을 합니다.

## v.0.3.16 - 2026-05-13

### 업데이트 내용

- 일정 생성/수정 팝업의 참석자 리스트를 닉네임 / 현재 상태 / 초대취소 버튼 구조로 정리했습니다.
- 초대 참석자의 `is_agree=false`는 `초대중`, `is_agree=true`는 `참석예정`으로 표시하도록 변경했습니다.
- 기존 삭제 버튼 문구를 초대 흐름에 맞게 `초대취소`로 변경했습니다.
- README/package 기준 버전을 `v.0.3.15`에서 소규모 패치 버전 `v.0.3.16`으로 업데이트했습니다.

### 작업 메모

- `events_invite.profile_id`가 현재 프로필이면 원본 `events.user_id`와 상관없이 내 초대 일정으로 병합되며, 수락 전은 주황색/수락 후는 파란색 표시 정책을 유지합니다.

## v.0.3.15 - 2026-05-13

### 업데이트 내용

- 코드 전체에서 남아 있던 `일정 있음` 숨김 문구를 제거하고, 월간/일간 화면 모두 `🔒닉네임` 표시로 통일했습니다.
- 월간 캘린더 이벤트 표시 로직을 `소유/초대 상태`, `상세 열람 가능 여부`, `표시 제목`, `색상` helper 함수로 분리해 중복 조건문을 정리했습니다.
- 초대 일정 주인이 현재 선택 그룹원 목록에 없을 때도 `profiles`를 추가 조회해 숨김 제목의 닉네임이 `이름 없음`으로 떨어지는 경우를 줄였습니다.
- README/package 기준 버전을 `v.0.3.14`에서 소규모 패치 버전 `v.0.3.15`로 업데이트했습니다.

### 작업 메모

- 리팩토링 범위는 일정 조회/표시 경로의 중복 제거와 숨김 문구 일관화에 집중했습니다. 구조가 큰 폼/댓글/초대 저장 로직은 다음 단계에서 컴포넌트/훅 분리를 권장합니다.

## v.0.3.14 - 2026-05-13

### 업데이트 내용

- FullCalendar 월간 헤더를 요청 형식인 `headerToolbar={{ left: 'prev', center: 'title', right: 'next' }}`로 변경했습니다.
- 초대받고 수락한 일정(`events_invite.is_agree=true`)은 내 일정처럼 파란색으로 표시하고, 미수락 초대(`false`)만 주황색으로 표시하도록 색상 분기를 바로잡았습니다.
- 숨김 타인 일정 제목을 `일정 있음`에서 `🔒닉네임` 형식으로 변경했습니다.
- README/package 기준 버전을 `v.0.3.13`에서 소규모 패치 버전 `v.0.3.14`로 업데이트했습니다.

### 작업 메모

- 초대받은 일정은 원본 `events` row를 추가 생성하지 않고 `events_invite.profile_id`가 현재 프로필인 row의 `event_id`로 표시합니다. 이벤트 ID가 겹치면 내 소유 일정, 초대받은 일정, 타인 일정 순으로 병합 우선순위를 둡니다.

## v.0.3.13 - 2026-05-13

### 업데이트 내용

- 초대받은 일정 조회를 `events_invite.profile_id` → `events_invite.event_id` → `events.id` 순서의 명시적인 2단계 조회로 변경했습니다.
- 현재 사용자 판정에 auth user id 대신 현재 프로필 id를 사용해 `events_invite.profile_id` 비교 기준을 맞췄습니다.
- 월간 캘린더 이전/다음 월 버튼이 제목 양옆에 가로로 배치되도록 toolbar chunk flex 스타일과 텍스트 화살표를 적용했습니다.
- README/package 기준 버전을 `v.0.3.12`에서 소규모 패치 버전 `v.0.3.13`으로 업데이트했습니다.

### 작업 메모

- `events_invite.is_agree=false`인 현재 프로필 초대도 필터링하지 않고 표시하며, 수락 전 확인이 쉽도록 일반 일정보다 우선 노출합니다.

## v.0.3.12 - 2026-05-13

### 업데이트 내용

- 미수락 초대받은 일정이 현재 사용자 기준 조회에 확실히 포함되도록 초대 조회 profile 범위를 보강했습니다.
- 미수락 초대받은 일정은 주황색으로 표시하고, 같은 날짜의 타인 일정과 겹쳐 노출 개수가 제한될 때 더 앞에 오도록 우선순위를 높였습니다.
- 월간 캘린더 상단 요일 헤더(`sun`, `mon`...)가 보이도록 전역 CSS 숨김 규칙을 제거했습니다.
- 월/년 제목 양옆에 이전/다음 월 이동 버튼을 추가했습니다.
- README/package 기준 버전을 `v.0.3.11`에서 소규모 패치 버전 `v.0.3.12`로 업데이트했습니다.

### 작업 메모

- 기본 정렬은 하루 종일 우선, 이후 시작 시간/제목 순서를 유지하되 미수락 초대는 수락 액션을 놓치지 않도록 일반 타인 일정보다 먼저 노출합니다.

## v.0.3.11 - 2026-05-13

### 업데이트 내용

- 월간 캘린더에서 긴일정을 이어진 막대가 아니라 날짜별 한 칸 일정으로 펼쳐 표시하도록 변경했습니다.
- 펼쳐진 긴일정 인스턴스는 모두 같은 원본 일정 ID에 연결되어, 어느 날짜에서 열어도 하나의 DB 일정 상세/삭제 흐름을 사용합니다.
- 월간 캘린더 정렬 우선순위를 `하루 종일 일정` 우선, 이후 시작 시간 오름차순/제목 오름차순으로 단순화했습니다.
- 색상 정책을 내 일정 파랑, 타인 일정 초록, 초대받은 일정 주황 기준으로 변경했습니다.
- README 기준 버전을 `v.0.3.10`에서 소규모 패치 버전 `v.0.3.11`로 업데이트했습니다.

### 작업 메모

- 월간 표시용 이벤트만 날짜별 인스턴스로 펼치고, 상세/수정/삭제에 사용하는 원본 일정 데이터는 기존 단일 row 구조를 유지했습니다.

## v.0.3.10 - 2026-05-13

### 업데이트 내용

- 월간 캘린더/리스트 정렬에 `긴일정` 개념과 사용자 지정 우선순위를 반영했습니다.
- 우선순위는 초대된 내 긴일정 → 내 긴일정 → 초대된 내 일반 일정 → 내 일반 일정 → 타인 긴일정 → 타인 일반 일정 순서입니다.
- 같은 일정이 여러 경로로 조회될 때 동일 우선순위 기준으로 하나만 표시되도록 중복 표시 우선순위도 맞췄습니다.
- 긴일정끼리는 시작일 빠른 순, 시작일이 같으면 종료일 늦은 순, 이후 제목 오름차순으로 정렬합니다.
- FullCalendar `eventOrderStrict`와 `eventOrder`를 설정해 긴일정과 일반 일정이 같은 라인에서 겹치지 않고 우선순위 순서로 분리 표시되도록 보강했습니다.
- README 주요 기능 섹션을 현재 구현된 인증/캘린더/긴일정/초대/색상/친구/그룹/댓글/RPC 기능 기준으로 상세 정리했습니다.
- README/package 기준 버전을 `v.0.3.9`에서 소규모 패치 버전 `v.0.3.10`으로 업데이트했습니다.

### 작업 메모

- 다일 이벤트는 렌더용 `orderPriority`, `longStartAt`, `longEndAt` 값을 확장 속성에 넣고 FullCalendar 정렬에 사용합니다.

## v.0.3.9 - 2026-05-13

### 업데이트 내용

- 월간 캘린더 드래그 선택 후 첫 번째 날짜에 하늘색 배경이 남던 문제를 수정했습니다.
- 드래그 종료/FullCalendar select 완료 시 `calendarApi.unselect()`를 호출하고, 선택 상태를 명시적으로 초기화합니다.
- 여러 날짜에 걸친 일정 막대가 첫 날짜 셀에서만 보이던 문제를 수정했습니다.
- 다일 일정 segment가 다음 날짜 셀까지 이어질 수 있도록 월간 셀/프레임/이벤트 영역의 horizontal clipping을 제거했습니다.
- README/package 기준 버전을 `v.0.3.8`에서 소규모 패치 버전 `v.0.3.9`로 업데이트했습니다.

### 작업 메모

- 원인은 `unselectAuto=false`와 커스텀 드래그 fallback이 함께 쓰이면서 FullCalendar의 native highlight가 남을 수 있었던 점, 그리고 `overflow:hidden`이 다일 일정 segment를 첫 셀에서 잘라내던 점입니다.

## v.0.3.8 - 2026-05-13

### 업데이트 내용

- 월간 캘린더 롱프레스 후 드래그 선택이 동작하도록 포인터 기반 범위 선택 fallback을 추가했습니다. 선택 중인 날짜 범위는 하늘색으로 표시되고, 손/마우스를 떼면 해당 범위로 일정 추가 팝업이 열립니다.
- 여러 날짜에 걸친 일정이 첫날에만 표시되던 문제를 수정했습니다. 월간 캘린더 렌더용으로 다일 일정은 all-day range 이벤트로 변환하고, FullCalendar의 exclusive end 규칙에 맞춰 종료일 다음 날을 end로 전달합니다.
- 일정 수정 팝업 참석자 리스트에서 `events_invite.is_agree=false`인 참석자도 표시하고 `초대중`으로 표기합니다. `true`가 되면 초대중 문구를 숨깁니다.
- 일정 소유자는 참석자 리스트에서 초대 참석자를 삭제할 수 있도록 유지했습니다.
- README/package 기준 버전을 `v.0.3.7`에서 소규모 패치 버전 `v.0.3.8`로 업데이트했습니다.

### 작업 메모

- 기존 FullCalendar select 옵션은 유지하되, 모바일 롱프레스/드래그가 환경별로 불안정할 수 있어 `elementFromPoint` 기반 포인터 선택을 추가했습니다.

## v.0.3.7 - 2026-05-13

### 업데이트 내용

- 일정 추가/수정 팝업 레이아웃을 이전 구조로 되돌렸습니다.
- 월간 캘린더 드래그 선택이 모바일/데스크톱에서 더 안정적으로 동작하도록 `selectMirror`, `unselectAuto=false`, 롱프레스 delay 옵션, `touch-action: none` CSS를 추가했습니다.
- 참석자가 있는 일정 저장에서 `save_event_with_invites` RPC가 DB에 적용되지 않은 경우, 더 이상 불명확한 초대 실패로 보이지 않게 명확한 안내 메시지를 띄우도록 했습니다.
- 초대가 없는 일정은 RPC가 없어도 기존 클라이언트 저장 fallback으로 저장됩니다. 참석자가 있는 일정은 DB 트랜잭션이 필요하므로 RPC 적용이 필요합니다.
- README/package 기준 버전을 `v.0.3.6`에서 소규모 패치 버전 `v.0.3.7`로 업데이트했습니다.

### 작업 메모

- 참석자 저장 프로세스: 폼에서 참석자를 메모리 상태에 추가 → 저장 클릭 → `save_event_with_invites` RPC로 events와 events_invite를 한 트랜잭션 저장 → RPC 미적용 시 참석자 포함 저장은 중단하고 안내합니다.

## v.0.3.6 - 2026-05-13

### 업데이트 내용

- 월간 캘린더 전체 높이를 이전 대비 약 3/4 수준으로 줄이고, 하루 이벤트 표시 개수를 3개로 복원했습니다.
- 줄어든 캘린더 높이에 맞춰 월간 행 높이와 이벤트/더보기 높이를 더 작게 보정했습니다.
- 일정 추가/수정 팝업을 큰 flex 상/하 구조로 재정렬했습니다. 상단은 `왼쪽: 일정제목+세부일정`, `오른쪽: 참석자`, 하단은 `시작날짜/시간 ~ 종료날짜/시간`, `체크박스 2개`, `버튼들` 순서입니다.
- 월간 캘린더 이벤트 요소는 닉네임 없이 제목 한 줄만 표시하도록 유지했습니다.
- 참석자 저장 실패 가능성이 큰 `events_invite` RLS/FK 환경을 고려해 RPC SQL을 `security definer`로 보강했습니다.
- RPC 내부에서 초대 대상이 자기 자신이 아니고 accepted 친구인 경우만 `events_invite`에 저장하도록 검증을 추가했습니다.
- README/package 기준 버전을 `v.0.3.5`에서 소규모 패치 버전 `v.0.3.6`으로 업데이트했습니다.

### 작업 메모

- 참석자 추가 시 계속 에러가 난다면 운영 Supabase에 최신 `supabase/save_event_with_invites.sql`을 다시 적용해야 합니다. 이 함수가 DB에 적용되어야 RLS 영향을 받지 않는 단일 트랜잭션 저장이 가능합니다.

## v.0.3.5 - 2026-05-13

### 업데이트 내용

- 월간 캘린더 이벤트 렌더를 한 줄로 고정하고 타인 일정 닉네임 표시를 제거해 요소 깨짐을 줄였습니다.
- 월간 캘린더 6주 행 높이와 이벤트/더보기 높이를 고정 CSS로 재정리했습니다.
- 일정 저장/수정 중 초대 저장이 실패하면 생성된 일정 삭제 또는 기존 일정/초대 목록 복원으로 롤백하도록 보강했습니다.
- 실제 DB 단일 트랜잭션 처리를 위한 `supabase/save_event_with_invites.sql` RPC 함수를 추가했습니다. 함수가 배포되어 있으면 앱은 RPC를 우선 사용하고, 없으면 클라이언트 롤백 흐름으로 fallback합니다.
- 초대 저장은 기존 `upsert(onConflict)` 대신 초대 목록 삭제 후 insert 방식으로 바꿔 composite key/onConflict 설정 차이에 따른 실패 가능성을 줄였습니다.
- 일정 추가/수정 팝업 레이아웃을 요청 구조에 맞춰 `일정제목 | 참석자`, `세부일정 | 리스트`, `시작날짜/시간 - 종료날짜/시간`, `체크박스` 순서로 정리했습니다.
- README/package 기준 버전을 `v.0.3.4`에서 소규모 패치 버전 `v.0.3.5`로 업데이트했습니다.

### 작업 메모

- Supabase 클라이언트만으로는 여러 테이블 쓰기를 진짜 DB 트랜잭션으로 묶을 수 없어서 RPC SQL을 함께 추가했습니다. 운영 DB에 SQL을 적용하면 완전한 트랜잭션으로 동작합니다.
- RPC 미배포 환경에서는 앱이 수동 롤백을 수행합니다.

## v.0.3.4 - 2026-05-13

### 업데이트 내용

- 일정 목록/상세/작성/친구 검색 팝업의 dim 영역을 클릭하면 팝업이 닫히도록 변경했습니다.
- 월간 캘린더를 6주 고정(`fixedWeekCount`)으로 표시하고, CSS로 모든 주 행 높이와 롱프레스 드래그 highlight 높이를 동일하게 보정했습니다.
- 앱 진입점(`/`)에서 세션 확인이 불안정하게 `/login`으로 떨어질 수 있는 회귀를 줄이기 위해 `getSession` + `getUser` + `INITIAL_SESSION` 기준 라우팅을 보강했습니다.
- 친구 추가 팝업을 `70vh` 고정 높이로 바꾸고, 검색 결과 리스트 영역만 스크롤되도록 정리했습니다.
- 일정 생성/수정 팝업에 참석자 리스트와 친구 초대 팝업을 추가했습니다. 일정 소유자는 참석자에 항상 포함되고, 친구 검색 결과 클릭 시 저장 전 참석자 목록에 추가됩니다.
- 일정 저장 시 참석자 목록을 `events_invite(event_id, profile_id, is_agree)`에 저장하고, 수정 시 기존 초대 목록을 갱신합니다.
- 월간 캘린더 조회가 `events`뿐 아니라 `events_invite`도 확인하도록 확장됐습니다.
- 초대 상태 색상 정책을 반영했습니다: 내 일정/수락한 내 초대는 파랑, 대기 중인 내 초대는 주황, 타인 일정/수락한 타인 초대는 초록, 미수락 타인 초대는 숨김.
- 같은 일정이 여러 경로로 보일 수 있을 때 내 일정 → 내 미수락 초대 → 내 수락 초대 → 타인 일정 → 타인 수락 초대 우선순위로 하나만 표시합니다.
- 초대받은 일정 상세에서 참석하기/참석거절/참석취소 버튼을 표시하고, `events_invite` 업데이트/삭제로 처리합니다.
- 일정 겹침 차단을 제거하고, 목록/캘린더 정렬을 초대받은 일정 → 하루종일 → 시작시간 오름차순 기준으로 변경했습니다.
- README/package 기준 버전을 `v.0.3.3`에서 소규모 패치 버전 `v.0.3.4`로 업데이트했습니다.

### 작업 메모

- 초대받은 일정은 별도 `events` row를 만들지 않고 `events_invite` row만 저장합니다.
- `events_invite` 관계 select는 Supabase FK 자동 관계(`event:events(*)`, `profile:profiles(...)`)를 사용합니다. DB FK명이 다르게 잡혀 있으면 select alias만 조정하면 됩니다.

## v.0.3.3 - 2026-05-12

### 업데이트 내용

- 월간 캘린더 바깥 라운드/그림자 껍데기를 제거하고, FullCalendar 이벤트 글자/높이/여백을 줄여 공간 효율을 높였습니다.
- 월간 캘린더의 이벤트를 클릭해도 개별 상세가 바로 열리지 않고 해당 날짜의 일정 목록 팝업이 열리도록 변경했습니다. 개별 상세는 목록 팝업 안의 일정 클릭으로만 열립니다.
- 날짜 일정 목록 팝업 높이를 `65vh`로 고정하고, 일정 목록 영역만 스크롤되도록 정리했습니다.
- `events.is_allday` 컬럼을 일정 생성/수정 payload에 반영하고, 하루 종일 일정은 목록에서 `하루 종일`로 표시합니다.
- 일정 생성/수정의 숨김 옵션을 `상세 일정 함께 보기`로 바꾸고, 기본값은 숨김(`is_hidden=true`)으로 맞췄습니다.
- 일정 생성/수정 폼에 시작/종료 날짜 input을 추가하고, 시작/종료 시간을 한 줄 양옆 배치로 변경했습니다. 체크박스 2개도 하단 양옆 배치로 정리했습니다.
- 타인의 일정을 월간 캘린더와 목록에서 볼 때 일정 제목 아래/옆에 작성자 닉네임을 표시합니다.
- 월간 캘린더 하단 오른쪽에 플로팅 `+` 버튼을 추가해 날짜까지 수정 가능한 일정 추가가 가능하도록 했습니다.
- 월간 캘린더 롱프레스 드래그 선택으로 여러 날짜를 선택해 하루 종일 범위 일정 추가가 가능하도록 했습니다.
- 여러 날짜에 걸친 일정 조회를 위해 월간 조회 조건을 `start_at` 기준 단일 포함이 아니라 가시 범위와 겹치는 일정 조회로 변경했습니다.
- README/package 기준 버전을 `v.0.3.2`에서 소규모 패치 버전 `v.0.3.3`으로 업데이트했습니다.

### 작업 메모

- DB 시간 저장은 기존 정책대로 `00:00 ~ 24:00` 패턴을 유지하며, UI 표시는 `is_allday` 값을 우선 사용합니다.
- FullCalendar의 날짜 범위 선택 `end`는 exclusive라서, 드래그 선택 시 마지막 표시 날짜는 `end - 1일`로 계산합니다.

## v.0.3.2 - 2026-05-12

### 업데이트 내용

- 월간 캘린더 초기 진입 시 FullCalendar가 mount되지 않아 `datesSet`이 호출되지 않고 로딩 화면에 머무르던 문제를 수정했습니다.
- 인증/프로필 조회가 끝난 뒤에는 FullCalendar를 먼저 mount하고, 일정 조회 중에는 캘린더 위에 로딩 오버레이를 덮도록 변경했습니다.
- 월간 캘린더에서 가시 범위(`datesSet`) 기준으로 일정 데이터를 조회하고 날짜 클릭 팝업/상세 팝업으로 확인하는 흐름이 유지되는지 확인했습니다.
- README/package 기준 버전을 `v.0.3.1`에서 소규모 패치 버전 `v.0.3.2`로 업데이트했습니다.

### 작업 메모

- 원인은 `isCalendarLoading`이 true일 때 FullCalendar 자체를 조건부 렌더링에서 제외해, 가시 범위를 만드는 `datesSet` 콜백이 영원히 실행될 수 없었던 구조였습니다.
- Day 페이지는 직접 URL 접근 호환용으로 남아 있으며, 캘린더 UI의 기본 데이터 확인/추가/상세 흐름은 `app/calendar/page.tsx`에서 처리합니다.

## v.0.3.1 - 2026-05-12

### 업데이트 내용

- 월간 캘린더를 본격적인 일정 보기 화면으로 개편했습니다. `app/calendar/page.tsx`가 가시 범위의 일정을 직접 조회하고 표시합니다.
- 본인 일정은 파랑(`#3B82F6`), 현재 대표 그룹의 다른 그룹원 일정은 초록(`#10B981`)으로 표시합니다. 숨김 일정은 60% opacity로 렌더링됩니다.
- 하루에 최대 4개까지 일정 막대를 노출하고, 그보다 많으면 `+N` 더보기 링크를 표시합니다. 더보기 클릭은 날짜 팝업으로 연결됩니다.
- 날짜를 클릭하면 일정 팝업이 열립니다. 상단은 날짜, 중앙은 시간 오름차순 일정 리스트, 하단은 `일정 추가` 버튼입니다.
- `일정 추가` 클릭 시 기본 시간 `09:00 ~ 22:00`으로 일정 폼 팝업이 열립니다.
- 일정 클릭 시 기존 상세/댓글 팝업이 그대로 동작합니다. 본인 일정은 상세에서 수정 가능, 그룹원의 숨김 일정은 클릭이 막힙니다.
- 캘린더 레이아웃을 `100vh - 6.5rem` 기준 flex 컬럼으로 변경해 하단 네비게이션을 침범하지 않고 화면을 꽉 채우도록 했습니다.
- README/package 기준 버전을 `v.0.2.8`에서 대규모 패치 버전 `v.0.3.1`로 업데이트했습니다.

### 작업 메모

- 일정 조회 범위는 FullCalendar `datesSet`이 알려주는 가시 범위(`arg.start`, `arg.end`)로 잡고, 그룹/범위가 바뀔 때만 재조회합니다. 같은 범위로 `datesSet`가 다시 호출되더라도 state 재설정으로 인한 불필요한 fetch가 발생하지 않게 비교 후 갱신합니다.
- Day 페이지(`app/day/[date]`)는 직접 URL 접근 시 동작하도록 유지했습니다. 캘린더 UI에서는 더 이상 사용하지 않으며, 후속 단계에서 제거 또는 정리를 검토합니다.
- 기본 시간 `09:00 ~ 22:00`은 사용자 요청 그대로이며, 사용자가 30분 단위 드롭다운에서 자유롭게 변경 가능합니다.

## v.0.2.8 - 2026-05-12

### 업데이트 내용

- 월간 캘린더 상단의 이전/다음 화살표(`prev,next`)를 제거했습니다.
- 시간 입력을 `<input type="time">` 대신 30분 단위 커스텀 드롭다운으로 변경했습니다.
- 시간 관련 유틸(`START_TIME_SLOTS`, `END_TIME_SLOTS`, `formatDisplayTime`, `formatEndTimeWithDuration`, `getValidEndSlots`, `dateToTimeValue`, `timeValueToDate`, `isAllDayEvent`)을 `lib/timeSlots.ts`로 분리했습니다.
- `components/TimeSelect.tsx` 컴포넌트를 추가해 시작 시간은 `오전 10:00`, 종료 시간은 `오후 1:00 (3시간)` 형식으로 표시하도록 했습니다.
- 일정 폼에 `하루 종일` 체크박스를 추가했습니다. 체크 시 시작=`00:00`, 종료=`24:00`으로 설정하고 드롭다운을 비활성화하며, 해제 시 이전 시간으로 복원합니다.
- README/package 기준 버전을 `v.0.2.7`에서 소규모 패치 버전 `v.0.2.8`로 업데이트했습니다.

### 작업 메모

- 종료 시간 슬롯은 선택된 시작 시간보다 늦은 값만 표시되므로 `start >= end` 상태로 저장될 수 없습니다.
- `24:00`은 `timeValueToDate`에서 `T23:59:59`로 변환되며, `isAllDayEvent`는 이 패턴으로 하루 종일 여부를 판별합니다.

## v.0.2.7 - 2026-05-12

### 업데이트 내용

- 설정 페이지 왼쪽 아래에 회원 탈퇴 버튼을 추가했습니다.
- 탈퇴 버튼 클릭 시 경고 모달을 표시하고, 확인 시 데이터 정리 후 로그인 페이지로 이동합니다.
- 탈퇴 처리 순서: ① friendships에서 requester_id/addressee_id 기준 전체 삭제 → ② owner 그룹 ID 조회 후 해당 그룹의 모든 멤버 row 삭제 → ③ 남은 그룹 멤버십 row 삭제 → ④ 세션 종료.
- `friends/page.tsx`의 `handleAcceptFriend`, `handleDeleteFriend`에서 `fetchFriends()` 호출에 `await`가 빠진 버그를 수정했습니다.

### 작업 메모

- 탈퇴 시 auth 계정 삭제는 Supabase admin API 권한이 필요하므로 현재는 세션 종료(signOut)만 수행합니다. 계정 자체 삭제가 필요하면 서버 사이드 Route Handler + admin client로 보강해야 합니다.
- owner 그룹 삭제는 해당 그룹의 모든 멤버 row를 group id 기준으로 삭제하는 방식입니다. DB 수준의 cascade가 없으면 events, comments 등 관련 데이터는 별도 정리가 필요합니다.

## v.0.2.6 - 2026-05-12

### 업데이트 내용

- Day 페이지 내부에서 그룹 변경 시 이전 프로필 캐시 기준으로 즉시 재조회되던 문제를 수정했습니다.
- 그룹 변경 시 상세/작성 팝업을 닫고 로딩 상태로 전환한 뒤, 변경된 `main_group_id` 기준으로 사람/일정을 다시 조회하도록 했습니다.
- FullCalendar 인스턴스에 그룹/사용자/날짜 기반 key를 부여해 그룹 변경 후 이전 캘린더 상태가 남지 않도록 했습니다.
- 친구 추가 팝업을 닉네임 검색 목록 방식으로 변경했습니다.
- 친구 검색은 `%검색어%`, 닉네임 오름차순, 20개 단위 페이지네이션으로 조회합니다.
- 검색 결과 클릭 시 친구 요청 확인창을 띄우고, 이미 친구/요청 상태면 `이미 친구인 사용자에요.` 알림을 표시합니다.
- 친구 추가 팝업의 하단 추가 버튼을 제거하고 취소 버튼을 오른쪽 위로 이동했습니다.
- README/package 기준 버전을 `v.0.2.5`에서 소규모 패치 버전 `v.0.2.6`으로 업데이트했습니다.

### 작업 메모

- Day 페이지 오동작 원인은 `GroupSelector`의 낙관적 프로필 업데이트 직후 기존 렌더의 `fetchEvents`가 실행되어 이전 `main_group_id`를 참조할 수 있었던 점입니다. 이제 그룹 변경은 프로필 캐시 변경 effect가 담당합니다.

## v.0.2.5 - 2026-05-12

### 업데이트 내용

- 루트(`/`) 페이지가 무조건 로그인 화면으로 이동하던 동작을 세션 확인 후 분기하도록 수정했습니다.
- 같은 기기/PWA 진입 시 Supabase 세션이 있으면 `/calendar`, 없으면 `/login`으로 이동합니다.
- 루트 세션 확인 중에는 공통 캘린더 로딩 화면을 표시합니다.
- README/package 기준 버전을 `v.0.2.4`에서 소규모 패치 버전 `v.0.2.5`로 업데이트했습니다.

### 작업 메모

- PWA 아이콘이나 도메인 직접 진입은 `/`로 시작하므로, 자동 로그인 UX는 루트 페이지에서 `getSession()` 기준으로 먼저 분기하는 것이 안전합니다.

## v.0.2.4 - 2026-05-12

### 업데이트 내용

- Supabase 브라우저 클라이언트에 `persistSession`, `autoRefreshToken`, `detectSessionInUrl` 옵션을 명시해 같은 기기 자동 로그인 유지 동작을 분명히 했습니다.
- 설정 페이지를 클라이언트 컴포넌트로 전환하고 오른쪽 아래에 로그아웃 버튼을 추가했습니다.
- 로그아웃 시 Supabase 세션을 종료하고 TanStack Query 캐시를 비운 뒤 로그인 페이지로 이동하도록 했습니다.
- README/package 기준 버전을 `v.0.2.3`에서 소규모 패치 버전 `v.0.2.4`로 업데이트했습니다.

### 작업 메모

- 실제 세션 만료/refresh token 정책은 Supabase Auth 설정이 최종 기준입니다. 앱에서는 같은 브라우저 저장소의 세션 유지와 자동 refresh를 명시했습니다.

## v.0.2.3 - 2026-05-12

### 업데이트 내용

- `@tanstack/react-query`, `@supabase/ssr`를 추가하고 앱 전체를 `QueryProvider`로 감쌌습니다.
- `useCurrentUser`, `useMyProfile`, `useMyAcceptedGroups` 훅을 추가해 접속 유저/프로필/수락 그룹 정보를 공통 캐시로 재사용하도록 했습니다.
- `GroupSelector`, 월간 캘린더, Day 페이지, 친구/그룹 페이지의 반복 `getUser` 호출을 공통 캐시 기반으로 줄였습니다.
- Supabase SSR `middleware.ts`를 추가해 서버 라우팅 중 Auth 쿠키가 갱신되도록 했습니다.
- `lib/supabase.ts`를 `createBrowserClient` 기준으로 변경해 SSR 쿠키 흐름과 맞췄습니다.
- 로컬 npm script 실행을 방해하던 `node` 런타임 패키지 의존성을 제거했습니다.
- README/package 기준 버전을 `v.0.2.2`에서 소규모 패치 버전 `v.0.2.3`으로 업데이트했습니다.

### 작업 메모

- 로그인 세션 만료 시간을 정확히 1시간으로 정하는 값은 코드가 아니라 Supabase Auth Dashboard의 JWT expiry 설정이 기준입니다. 이번 변경은 앱에서 세션 쿠키 갱신과 캐싱을 안정화하는 역할입니다.
- Next.js 16 빌드에서 `middleware.ts` convention deprecation 경고가 표시됩니다. 현재 동작은 정상이며, 추후 Next 권장 방식인 `proxy.ts`로 전환하면 경고를 제거할 수 있습니다.

## v.0.2.2 - 2026-05-12

### 업데이트 내용

- 친구 제한을 `FRIEND_LIMIT = 100`으로 늘리고 친구관리 화면의 최대 3명 안내 문구를 제거했습니다.
- 하단 네비게이션 표시를 텍스트에서 이모지 아이콘으로 변경했습니다.
- 월간 캘린더와 Day 페이지의 FullCalendar 렌더 전 최소 1초 로딩 화면을 추가했습니다.
- README/package 기준 버전을 `v.0.2.1`에서 소규모 패치 버전 `v.0.2.2`로 업데이트했습니다.

### 작업 메모

- Supabase 클라이언트 쿼리는 기본적으로 앱 레벨 캐시가 자동 적용되지 않습니다. 자주 쓰는 그룹/프로필 조회는 React state 공유, TanStack Query/SWR, Next 서버 라우트의 `fetch` 캐시, 또는 DB index/RPC로 최적화하는 편이 좋습니다.

## v.0.2.1 - 2026-05-12

### 업데이트 내용

- 그룹 정책 상수(`GROUP_LIMIT`, `GROUP_MEMBER_LIMIT`, `GROUP_NAME_MAX_LENGTH`)와 그룹 유틸을 `lib/groups.ts`로 분리했습니다.
- 그룹관리 페이지를 추가해 그룹 목록, 초대 수락/거절, 탈퇴, 생성, 이름 중복 체크를 지원하도록 했습니다.
- 그룹 상세/수정 팝업을 추가해 소유자 기준 이름 수정, 그룹 삭제, 그룹원 추가, 위임, 추방을 처리하도록 했습니다.
- 그룹 삭제/탈퇴/추방 시 `profiles.main_group_id`가 해당 그룹이면 `null`로 초기화하도록 반영했습니다.
- 하단 고정 네비게이션을 추가하고 기존 캘린더/친구 이동 버튼을 제거했습니다.
- 설정 페이지를 신규 추가하고 임시 안내 텍스트를 표시했습니다.
- 월간 캘린더와 Day 페이지에 그룹 선택 드롭다운을 추가해 `profiles.main_group_id`를 업데이트하도록 했습니다.
- Day 페이지 일정 조회 기준을 친구 목록에서 대표 그룹의 수락 완료 멤버로 변경하고, `main_group_id`가 없으면 내 일정만 보이도록 했습니다.
- README/package 기준 버전을 큰 변경 기준에 맞춰 `v.0.1.3`에서 `v.0.2.1`로 업데이트했습니다.

### 작업 메모

- `profiles.main_group_id` 컬럼이 nullable로 존재해야 하며, `groups.id` + `groups.profile_id` 복합 키 구조를 기준으로 같은 `id`의 여러 멤버 row를 관리합니다.
- 그룹 제한은 클라이언트 UX 가드입니다. 동시 초대/직접 API 호출까지 막으려면 Supabase RLS, unique constraint, trigger/RPC 보강이 필요합니다.

## v.0.1.3 - 2026-05-12

### 업데이트 내용

- 댓글 최대 길이를 `COMMENT_MAX_LENGTH = 100` 상수로 분리했습니다.
- 댓글 작성과 수정 모두 같은 100자 제한 검증을 사용하도록 반영했습니다.
- 댓글 작성 input과 수정 textarea에 `maxLength`를 적용하고 글자 수 카운터를 추가했습니다.
- README 기준 버전을 `v.0.1.2`에서 소규모 패치 버전 `v.0.1.3`으로 업데이트했습니다.

### 작업 메모

- DB 길이 제한과 프론트 상수 값이 어긋나지 않도록 이후 댓글 정책 변경 시 `COMMENT_MAX_LENGTH`를 함께 수정해야 합니다.

## v.0.1.2 - 2026-05-12

### 업데이트 내용

- 일정 세부내용 DB 컬럼명을 `detaile`에서 `detail` 기준으로 수정했습니다.
- 일정 생성/수정 payload, 조회 타입, 상세 팝업 표시를 모두 `events.detail`에 맞췄습니다.
- 내 댓글에만 수정/삭제 버튼이 보이도록 댓글 권한 UI를 추가했습니다.
- 댓글 수정 시 `comments.contents`와 `modified_at`을 업데이트하도록 처리했습니다.
- 댓글 삭제 시 `id`, `events_id`, `profile_id`를 함께 조건으로 사용해 내 댓글만 삭제되도록 처리했습니다.
- README 기준 버전을 `v.0.1.1`에서 소규모 패치 버전 `v.0.1.2`로 업데이트했습니다.

### 작업 메모

- 클라이언트에서 내 댓글만 버튼을 보여주고 update/delete 조건에도 `profile_id`를 포함했습니다. 실제 권한 강제는 Supabase RLS 정책으로도 보강하는 것이 안전합니다.

## v.0.1.1 - 2026-05-12

### 업데이트 내용

- 숨김 일정 표시 opacity를 약 80%에서 약 60%로 낮췄습니다.
- 일정 생성/수정 모달에 선택 입력인 세부내용 textarea를 추가하고 `events.detail`에 저장하도록 반영했습니다.
- 세부내용은 최대 500자로 제한하고, 제목은 기존처럼 필수/최대 50자 검증을 유지했습니다.
- 일정 클릭 시 게시글 형태의 상세 팝업을 열도록 변경했습니다.
- 상세 팝업에 닫기 버튼, 내 일정 전용 수정 버튼, 제목, 날짜/시간, 세부내용, 댓글 입력, 댓글 목록을 추가했습니다.
- 댓글은 `comments` 테이블에 저장하고, `created_at` 오름차순으로 작성자 닉네임/내용/작성일을 표시합니다.
- 일정 삭제 시 `comments` FK 제약을 고려해 연결 댓글을 먼저 삭제하도록 처리했습니다.
- 상대방 숨김 일정(`events.is_hidden = true`)은 클릭해도 상세 팝업이 열리지 않도록 막았습니다.
- 일정 수정은 캘린더 이벤트 직접 클릭 모달이 아니라 상세 팝업의 내 일정 전용 수정 버튼에서 시작하도록 흐름을 변경했습니다.
- README 기준 버전을 큰 업데이트 기준에 맞춰 `v.0.0.6`에서 `v.0.1.1`로 업데이트했습니다.

### 작업 메모

- 숨김 일정의 실제 title/detail 데이터 비공개까지 필요하면 Supabase RLS/RPC 응답 마스킹이 추가로 필요합니다.

## v.0.0.6 - 2026-05-11

### 업데이트 내용

- 숨김 일정 DB 컬럼명을 `isHidden`에서 `is_hidden` 기준으로 수정했습니다.
- 일정 생성/수정 payload와 조회 타입, 표시 조건을 모두 `events.is_hidden`에 맞췄습니다.
- README 기준 버전을 `v.0.0.5`에서 소규모 패치 버전 `v.0.0.6`으로 업데이트했습니다.

### 작업 메모

- React state 이름은 컴포넌트 내부 관례에 맞춰 `isHidden`을 유지하고, Supabase와 주고받는 데이터 키만 DB 컬럼명인 `is_hidden`으로 매핑했습니다.

## v.0.0.5 - 2026-05-11

### 업데이트 내용

- 일정 추가/수정 모달에 `친구에게 숨기기` 체크박스를 추가했습니다.
- 체크된 일정은 `events.is_hidden` 값이 `true`로 저장/수정되도록 반영했습니다.
- 숨김 일정은 본인에게 실제 제목으로, 친구에게는 상수 `HIDDEN_EVENT_TITLE`의 `일정 있음` 텍스트로 표시되도록 처리했습니다.
- 숨김 일정의 FullCalendar 이벤트 색상은 80% opacity hex alpha를 적용하도록 분리했습니다.
- README 기준 버전을 `v.0.0.4`에서 소규모 패치 버전 `v.0.0.5`로 업데이트했습니다.

### 작업 메모

- 현재 클라이언트는 친구 일정 조회 시 원본 title도 함께 받아온 뒤 화면 표시만 마스킹합니다. 실제 데이터 비공개까지 필요하면 Supabase RLS/RPC에서 타인 조회 응답의 title을 서버 단에서 마스킹해야 합니다.

## v.0.0.4 - 2026-05-11

### 업데이트 내용

- 전체 lint 실패 원인을 정리하고 `npm run lint`가 통과하도록 수정했습니다.
- `app/day/[date]/page.tsx`의 FullCalendar 핸들러에서 `any` 타입을 제거하고 명시 타입을 적용했습니다.
- 이벤트 클릭/드래그/리사이즈에서 nullable 날짜값을 방어 처리했습니다.
- `fetchVisiblePeople`, `fetchEvents`를 `useCallback`으로 감싸 hook dependency 경고를 해결했습니다.
- auth callback effect에 `router` 의존성을 추가했습니다.
- README 기준 버전을 `v.0.0.3`에서 소규모 패치 버전 `v.0.0.4`로 업데이트했습니다.

### 작업 메모

- lint 실패 원인은 기존 `any` 타입, 미사용 핸들러, hook dependency 누락, nullable event date 미처리였습니다.
- `npm run lint`와 `npx tsc --noEmit` 모두 통과했습니다.

## v.0.0.3 - 2026-05-11

### 업데이트 내용

- 친구 수 계산 로직을 `lib/friendships.ts`의 재사용 함수로 분리했습니다.
- 친구 제한 기준값을 `FRIEND_LIMIT` 상수로 분리해 화면 문구와 검증 로직이 같은 값을 사용하도록 정리했습니다.
- 클로드코드 협업을 위해 친구 수 계산 함수에 정책 의도와 서버 단 보강 필요성을 주석으로 남겼습니다.
- 앱 명칭을 영어 `OURCAL`, 한글 `우리캘린더` 기준으로 정리했습니다.
- README 기준 버전을 `v.0.0.2`에서 소규모 패치 버전 `v.0.0.3`으로 업데이트했습니다.

### 작업 메모

- 레포 이름은 변경하지 않았습니다.
- 클라이언트 친구 수 제한은 UX 가드입니다. 동시 요청/직접 API 호출 방지는 Supabase RPC, DB trigger, constraint 등 서버 단 정책 보강이 필요합니다.

## v.0.0.2 - 2026-05-11

### 업데이트 내용

- README 기준 버전 `v.0.0.1`에서 소규모 패치 버전 `v.0.0.2`로 업데이트했습니다.
- README에 버전 관리 규칙을 추가했습니다.
- README의 주요 기능에 친구 수 제한 정책에 따른 친구 요청 차단 내용을 추가했습니다.

### 작업 메모

- 버전 기준은 README에 명시된 버전을 우선합니다.
- 이후 버전 변경 시 `README.md`, `OPENCLAW.md`, `package.json`, `package-lock.json`을 함께 갱신합니다.
